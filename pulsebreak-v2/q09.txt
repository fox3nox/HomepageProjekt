 topY, 56, 22);","      ctx.fillRect(x - 22, topY + 22, 44, 12);","      for (let s = -1; s <= 1; s += 1) {","        const sx = x + s * 14;","        ctx.fillStyle = rgbaFromHex(theme.hazardBase, 0.82);","        ctx.beginPath(); ctx.moveTo(sx - 6, topY + 34); ctx.lineTo(sx, topY + 48); ctx.lineTo(sx + 6, topY + 34); ctx.closePath(); ctx.fill();","      }","      ctx.restore();","    }","  }","","  const playerScreenX = worldToScreenX(player.x + player.width / 2);","  const waveAlpha = Math.max(0, beat * beat) * (0.045 + drive * 0.15);","  if (waveAlpha > 0.004) {","    ctx.save(); ctx.globalCompositeOperation = 'lighter';","    for (let i = 0; i < 3; i += 1) {","      ctx.globalAlpha = waveAlpha * (1 - i * 0.22);","      ctx.strokeStyle = i % 2 === 0 ? theme.cyan : theme.magenta;","      ctx.lineWidth = 1.5 + i * 0.6;","      ctx.beginPath(); ctx.arc(playerScreenX, player.y + player.height/2, 24 + beat*34 + i*12, 0, Math.PI * 2); ctx.stroke();","    }","    ctx.restore();","  }","","  const scanOffset = -((cameraX * (0.22 + drive * 0.25)) % 260);","  ctx.strokeStyle = rgbaFromHex(currentDirectorSection >= 3 ? theme.amber : theme.cyan, 0.04 + drive * 0.06);","  ctx.lineWidth = 2;","  for (let i = -1; i < 6; i += 1) {","    const x = scanOffset + i * 260;","    ctx.beginPath(); ctx.moveTo(x, 66); ctx.lineTo(x + 112, VIEW_HEIGHT - 110); ctx.stroke();"]],[3203,3204,["    ctx.globalAlpha = passed ? 0.14 : 0.5 + beatPulse * 0.18;"]],[3247,3247,["function drawBrickRows(x, y, width, height, strokeStyle) {","  const brickH = 12;","  const brickW = 22;","  ctx.strokeStyle = strokeStyle;","  ctx.lineWidth = 1;","  for (let yy = y + 8; yy < y + height - 4; yy += brickH) {","    ctx.beginPath();","    ctx.moveTo(x + 1, yy + 0.5);","    ctx.lineTo(x + width - 1, yy + 0.5);","    ctx.stroke();","    const rowOffset = Math.floor((yy - y) / brickH) % 2 === 0 ? 0 : brickW / 2;","    for (let xx = x + rowOffset; xx < x + width; xx += brickW) {","      ctx.beginPath();","      ctx.moveTo(xx + 0.5, yy - brickH + 1);","      ctx.lineTo(xx + 0.5, yy + 0.5);","      ctx.stroke();","    }","  }","}",""]],[3251,3252,["  const scene = themeScene();","  const stoneA = moving ? theme.panel2 : (theme.stoneA || theme.panel);","  const stoneB = moving ? theme.violet : (theme.stoneB || theme.panel2);","  const trim = moving ? theme.lime : (theme.trim || theme.cyan);","  const hazardGlow = 