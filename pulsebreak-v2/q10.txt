 scene === \"inferno\" ? theme.amber : trim;"]],[3254,3257,["  ctx.shadowBlur = moving ? 22 : 14;","  ctx.shadowColor = rgbaFromHex(trim, moving ? 0.85 : 0.58);","  const grad = ctx.createLinearGradient(x, solid.y, x, solid.y + solid.height);","  grad.addColorStop(0, stoneB);","  grad.addColorStop(1, stoneA);","  ctx.fillStyle = grad;"]],[3260,3262,["  drawBrickRows(x, solid.y, solid.width, solid.height, rgbaFromHex('#ffffff', 0.10));","  ctx.fillStyle = trim;","  ctx.fillRect(x, solid.y, solid.width, moving ? 6 : 5);","  ctx.fillStyle = rgbaFromHex(hazardGlow, 0.22);","  ctx.fillRect(x, solid.y + solid.height - 6, solid.width, 6);","  ctx.strokeStyle = rgbaFromHex(trim, 0.24);","  ctx.lineWidth = 1.5;","  ctx.strokeRect(x + 2.5, solid.y + 2.5, Math.max(1, solid.width - 5), Math.max(1, solid.height - 5));"]],[3263,3275,["  if (solid.kind !== 'ground') {","    const capY = solid.y - 2;","    for (let sx = x + 8; sx < x + solid.width - 10; sx += 22) {","      ctx.fillStyle = rgbaFromHex(trim, 0.22);","      ctx.fillRect(sx, capY, 10, 2);"]],[3276,3276,["    const emblemX = x + solid.width / 2;","    const emblemY = solid.y + solid.height / 2 + 4;","    ctx.save(); ctx.globalAlpha = 0.46;","    ctx.fillStyle = scene === 'glacier' ? theme.cyan : (scene === 'arcane' ? theme.lime : theme.amber);","    ctx.beginPath(); ctx.moveTo(emblemX - 11, emblemY + 4); ctx.lineTo(emblemX, emblemY - 9); ctx.lineTo(emblemX + 11, emblemY + 4); ctx.lineTo(emblemX, emblemY + 11); ctx.closePath(); ctx.fill();","    ctx.restore();"]],[3277,3284,["    const step = 64;","    for (let sx = x - (x % step) - step; sx < x + solid.width + step; sx += step) {","      ctx.fillStyle = rgbaFromHex(trim, 0.10);","      ctx.fillRect(sx + 10, solid.y + 12, 36, 3);","      ctx.fillRect(sx + 20, solid.y + 15, 16, 4);","      ctx.fillRect(sx + 24, solid.y + 19, 8, 24);"]],[3293,3293,["  const base = theme.hazardBase || theme.amber || theme.magenta;","  const glow = theme.hazardGlow || theme.magenta;"]],[3295,3298,["  const grad = ctx.createLinearGradient(x, spike.y + (down ? 0 : spike.height), x, spike.y + (down ? spike.height : 0));","  grad.addColorStop(0, down ? glow : base);","  grad.addColorStop(1, down ? base : glow);","  ctx.shadowBlur = 20;","  ctx.shadowColor = rgbaFromHex(glow, 0.95);","  ctx.fillStyle = grad;"]],[3312,3314,["  ctx.strokeStyle = rgbaFromHex(\"#ffffff\", 0.6);","  ctx.lineWi