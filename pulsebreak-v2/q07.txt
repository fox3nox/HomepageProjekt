velocityX) - SPEEDS.SLOW) / (SPEEDS.FAST - SPEEDS.SLOW)));","  if (speedFactor > 0.04) {","    ctx.strokeStyle = rgbaFromHex(theme.glow || theme.cyan, 0.035 + speedFactor * 0.075);","    ctx.lineWidth = 1 + speedFactor * 2;","    const streakOffset = -((cameraX * (0.66 + speedFactor * 0.55)) % 190);","    for (let i = 0; i < 12; i += 1) {","      const y = 84 + ((i * 47) % Math.max(120, fluidTop - 94));","      const x = streakOffset + i * 150;","      const len = 44 + speedFactor * 120;","      ctx.beginPath(); ctx.moveTo(x, y); ctx.lineTo(x + len, y); ctx.stroke();","    }","  }","","  if (directorEnergy > 0.42) {","    ctx.save(); ctx.globalAlpha = (directorEnergy - 0.42) * 0.13; ctx.fillStyle = currentDirectorSection >= 3 ? theme.magenta : theme.violet; ctx.fillRect(0,0,VIEW_WIDTH,VIEW_HEIGHT); ctx.restore();","  }","}","","function drawSignalArchitecture() {","  const progress = currentProgress();","  const drive = directorDrive(progress);","  const phase = beatPhaseAtTime(simulationTime, world.bpm, world.music?.beatOffset ?? 0);","  const beat = 1 - phase;","  const scene = themeScene();","  const fluidTop = VIEW_HEIGHT - 64;","","  const baseWorld = Math.floor((cameraX * 0.42) / 210) * 210;","  for (let i = -2; i < 10; i += 1) {","    const wx = baseWorld + i * 210;","    const x = wx - cameraX * 0.42;","    const seed = seeded(wx * 0.013);","    const pillarH = 86 + seed * 104;","    const pillarY = GROUND_Y - pillarH - 8;"]],[3185,3188,["    ctx.globalAlpha = 0.25 + seed * 0.08;","    ctx.fillStyle = rgbaFromHex(theme.stoneB, 0.46);","    ctx.fillRect(x + 12, pillarY, 24, pillarH);","    ctx.fillRect(x + 4, pillarY - 12, 40, 14);","    ctx.fillRect(x + 2, pillarY + pillarH - 5, 44, 12);","    ctx.fillStyle = rgbaFromHex(theme.trim, 0.22);","    ctx.fillRect(x + 10, pillarY + 8, 28, 3);"]],[3189,3189,["  }","","  // Big repeated setpieces close to the reference feel.","  const setpieceBase = Math.floor(cameraX / 430) * 430;","  for (let i = -1; i < 6; i += 1) {","    const wx = setpieceBase + i * 430;","    const x = worldToScreenX(wx + 220);","    if (x < -220 || x > VIEW_WIDTH + 220) continue;","    const seed = seeded(wx * 0.17);","    if (scene === 'glacier') {","      ctx.save();","      ctx.globalAlpha = 0.36;","      ctx.fillStyle = rgbaFromHex(theme.stoneB, 0.36);","      ctx.fillRect(x - 78, fluidTop - 150, 156, 84);","      ctx.fillRect(x -