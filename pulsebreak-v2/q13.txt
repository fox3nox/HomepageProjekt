  const vignette = ctx.createRadialGradient(VIEW_WIDTH * 0.5, VIEW_HEIGHT * 0.45, VIEW_HEIGHT * 0.15, VIEW_WIDTH * 0.5, VIEW_HEIGHT * 0.45, VIEW_HEIGHT * 0.78);","  vignette.addColorStop(0, \"rgba(0,0,0,0)\");","  vignette.addColorStop(1, \"rgba(0,0,0,0.26)\");","  ctx.fillStyle = vignette;","  ctx.fillRect(0, 0, VIEW_WIDTH, VIEW_HEIGHT);","","  // Top cavern / temple silhouette.","  ctx.fillStyle = rgbaFromHex(theme.void || \"#000\", 0.34);","  ctx.beginPath();","  ctx.moveTo(0, 0);","  ctx.lineTo(0, 42);","  for (let x = 0; x <= VIEW_WIDTH; x += 28) {","    const y = 34 + Math.sin((x + cameraX * 0.3) * 0.017) * 6 + Math.sin((x + cameraX) * 0.041) * 4;","    ctx.lineTo(x, y);","  }","  ctx.lineTo(VIEW_WIDTH, 0);","  ctx.closePath();","  ctx.fill();","","  // Front particles.","  for (let i = 0; i < 28; i += 1) {","    const seed = i * 0.137;","    const x = fract(seed + cameraX * 0.00021 + simulationTime * (scene === \"inferno\" ? 0.015 : -0.008)) * VIEW_WIDTH;","    let y;","    let color;","    let alpha;","    if (scene === \"glacier\") {","      y = fract(seed * 2.7 + simulationTime * 0.06) * VIEW_HEIGHT;","      color = theme.white;","      alpha = 0.34;","    } else if (scene === \"arcane\") {","      y = fract(seed * 3.3 + simulationTime * 0.03) * (VIEW_HEIGHT * 0.8);","      color = i % 2 === 0 ? theme.magenta : theme.cyan;","      alpha = 0.22 + pulse * 0.12;","    } else {","      y = VIEW_HEIGHT - fract(seed * 1.9 + simulationTime * 0.07) * (VIEW_HEIGHT * 0.72);","      color = i % 2 === 0 ? theme.amber : theme.magenta;","      alpha = 0.26 + pulse * 0.1;","    }","    ctx.fillStyle = rgbaFromHex(color, alpha);","    const size = scene === \"arcane\" ? 3 : 2;","    ctx.fillRect(x, y, size, size);","  }","}",""]],[3794,3794,[""]],[3798,3798,["  drawSignalArchitecture();"]],[3809,3809,["  drawForegroundAtmosphere();"]],[3815,3816,["  setText(statusEl, debugHitboxes ? `${gameState.toUpperCase()} · ${runType.toUpperCase()} · ${player.mode.toUpperCase()} · ${player.velocityX.toFixed(0)} px/s` : \"\");"]]]