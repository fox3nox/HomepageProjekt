ean.split(\"\").map((c) => c + c).join(\"\") : clean;","  const int = parseInt(full, 16);","  const r = (int >> 16) & 255;","  const g = (int >> 8) & 255;","  const b = int & 255;","  return `rgba(${r},${g},${b},${alpha})`;","}","","function fract(value) {","  return value - Math.floor(value);","}","","function seeded(seed) {","  return fract(Math.sin(seed * 91.713) * 43758.5453123);","}","","function pathHill(yBase, amplitude, frequency, offset, rough = 0.35) {","  ctx.beginPath();","  ctx.moveTo(-40, VIEW_HEIGHT);","  for (let x = -40; x <= VIEW_WIDTH + 40; x += 18) {","    const t = (x + offset) * frequency;","    const wave = Math.sin(t) + Math.sin(t * 0.47 + 1.3) * rough;","    const y = yBase + wave * amplitude;","    ctx.lineTo(x, y);","  }","  ctx.lineTo(VIEW_WIDTH + 40, VIEW_HEIGHT);","  ctx.closePath();","}","","function drawChain(x, top, bottom, alpha = 0.22) {","  ctx.save();","  ctx.strokeStyle = rgbaFromHex(theme.trim || theme.cyan, alpha);","  ctx.lineWidth = 3;","  ctx.lineCap = \"round\";","  for (let y = top; y < bottom; y += 18) {","    ctx.beginPath();","    ctx.ellipse(x, y + 9, 5.5, 9, 0, 0, Math.PI * 2);","    ctx.stroke();","  }","  ctx.restore();","}","","function drawTorchProp(x, y, scale = 1) {","  const flame = theme.torch || theme.amber || \"#ffd46f\";","  ctx.save();","  ctx.translate(x, y);","  ctx.fillStyle = rgbaFromHex(theme.stoneA || theme.panel, 0.95);","  ctx.fillRect(-3 * scale, -6 * scale, 6 * scale, 18 * scale);","  ctx.shadowBlur = 18 * scale;","  ctx.shadowColor = flame;","  const grad = ctx.createLinearGradient(0, -18 * scale, 0, 0);","  grad.addColorStop(0, rgbaFromHex(\"#fff9d1\", 0.95));","  grad.addColorStop(0.5, rgbaFromHex(flame, 0.92));","  grad.addColorStop(1, rgbaFromHex(theme.magenta || \"#ff5f9e\", 0.42));","  ctx.fillStyle = grad;","  ctx.beginPath();","  ctx.moveTo(0, -24 * scale);","  ctx.quadraticCurveTo(10 * scale, -13 * scale, 0, -2 * scale);","  ctx.quadraticCurveTo(-9 * scale, -13 * scale, 0, -24 * scale);","  ctx.fill();","  ctx.restore();","}","","function drawArcaneBolt(x1, y1, x2, y2, color, alpha = 0.55) {","  ctx.save();","  ctx.strokeStyle = rgbaFromHex(color, alpha);","  ctx.lineWidth = 2;","  ctx.beginPath();","  ctx.moveTo(x1, y1);","  const steps = 5;","  for (let i = 1; i < steps; i += 1) {","    const t = i / steps;","    const x = x1 + (x2 - x1) * t + (i % 2 === 0 ? 10 : -10);","    