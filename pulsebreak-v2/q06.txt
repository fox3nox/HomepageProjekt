","    } else {","      ctx.fillStyle = rgbaFromHex(theme.ruin, 0.18 + seed * 0.09);","      ctx.fillRect(x + 24, GROUND_Y - 180 - seed * 34, 86, 138 + seed*40);","      ctx.fillRect(x + 8, GROUND_Y - 148 - seed * 34, 118, 24);","      ctx.fillRect(x + 26, GROUND_Y - 216 - seed * 34, 82, 26);","      drawTempleWindow(x + 46, GROUND_Y - 130 - seed * 16, 40, 40, theme.amber);"]],[3183,3184,["  if (scene !== 'glacier') {","    const chainOffset = -((cameraX * 0.29) % 170);","    for (let i = -1; i < 9; i += 1) {","      drawChain(chainOffset + i * 170 + 80, 42 + (i % 3) * 18, GROUND_Y - 34, scene === 'inferno' ? 0.18 : 0.14);","    }","  }","","  // Atmosphere particles.","  for (let i = 0; i < 52; i += 1) {","    const px = fract(i * 0.173 + cameraX * 0.00013) * VIEW_WIDTH;","    const py = 30 + fract(i * 0.287 + 0.21 + simulationTime * (scene === 'glacier' ? 0.01 : -0.004)) * (VIEW_HEIGHT * 0.68);","    const size = (i % 4 === 0) ? 2.5 : 1.5;","    const color = scene === 'inferno' ? (i % 2 === 0 ? theme.amber : theme.magenta) : (scene === 'arcane' ? (i % 2 === 0 ? theme.cyan : theme.magenta) : theme.white);","    ctx.fillStyle = rgbaFromHex(color, 0.18 + pulse * 0.18);","    ctx.fillRect(px, py, size, size);","  }","","  // Bottom fluid strip like water / energy / lava.","  const fluidGrad = ctx.createLinearGradient(0, fluidTop, 0, VIEW_HEIGHT);","  fluidGrad.addColorStop(0, theme.liquidTop);","  fluidGrad.addColorStop(1, theme.liquidBottom);","  ctx.fillStyle = fluidGrad;","  ctx.fillRect(0, fluidTop, VIEW_WIDTH, VIEW_HEIGHT - fluidTop);","  ctx.strokeStyle = rgbaFromHex(theme.glow || theme.trim, 0.8);","  ctx.lineWidth = 3;","  ctx.beginPath();","  for (let x = -20; x <= VIEW_WIDTH + 20; x += 14) {","    const y = fluidTop + Math.sin((x + cameraX * 0.75) * 0.03 + simulationTime * 3.1) * 4 + Math.sin(x * 0.012) * 2.5;","    if (x === -20) ctx.moveTo(x, y); else ctx.lineTo(x, y);","  }","  ctx.stroke();","","  if (scene === 'inferno') {","    for (let i = 0; i < 10; i += 1) {","      const x = fract(i * 0.213 + simulationTime * 0.03) * VIEW_WIDTH;","      const h = 16 + (i % 5) * 8;","      ctx.fillStyle = rgbaFromHex(theme.amber, 0.18 + pulse * 0.08);","      ctx.beginPath(); ctx.moveTo(x, fluidTop + 6); ctx.lineTo(x+8, fluidTop-h); ctx.lineTo(x+16, fluidTop + 6); ctx.fill();","    }","  }","","  const speedFactor = Math.max(0, Math.min(1, (Math.abs(player.