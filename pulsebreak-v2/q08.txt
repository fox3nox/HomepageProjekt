 46, fluidTop - 188, 92, 40);","      ctx.fillRect(x - 98, fluidTop - 66, 196, 12);","      ctx.restore();","      for (let c = -3; c <= 3; c += 1) {","        const cx = x + c * 18;","        const h = 18 + (3 - Math.abs(c)) * 16 + seed * 12;","        ctx.fillStyle = rgbaFromHex(theme.cyan, 0.52);","        ctx.beginPath(); ctx.moveTo(cx - 10, fluidTop - 6); ctx.lineTo(cx, fluidTop - 6 - h); ctx.lineTo(cx + 10, fluidTop - 6); ctx.closePath(); ctx.fill();","      }","      drawTorchProp(x - 58, fluidTop - 12, 0.8);","      drawTorchProp(x + 58, fluidTop - 12, 0.8);","    } else if (scene === 'arcane') {","      drawOrbStructure(x - 60, 210, 18, theme.cyan, theme.cyan);","      drawOrbStructure(x, 182, 22, theme.lime, theme.white || '#ffffff');","      drawOrbStructure(x + 60, 210, 18, theme.violet, theme.magenta);","      if ((Math.floor(simulationTime * 2 + i) % 2) === 0) {","        ctx.strokeStyle = rgbaFromHex(theme.cyan, 0.42 + drive * 0.2);","        ctx.lineWidth = 2.5;","        ctx.beginPath(); ctx.moveTo(x - 42, 205); ctx.lineTo(x - 12, 188); ctx.lineTo(x + 8, 188); ctx.lineTo(x + 42, 205); ctx.stroke();","      }","    } else {","      ctx.save();","      ctx.globalAlpha = 0.34;","      const grad = ctx.createLinearGradient(x, fluidTop - 170, x, fluidTop - 20);","      grad.addColorStop(0, theme.stoneA); grad.addColorStop(1, theme.stoneB);","      ctx.fillStyle = grad;","      ctx.fillRect(x - 80, fluidTop - 126, 160, 74);","      ctx.fillRect(x - 46, fluidTop - 164, 92, 38);","      ctx.fillRect(x - 106, fluidTop - 54, 212, 10);","      ctx.restore();","      drawTorchProp(x - 64, fluidTop - 12, 0.95);","      drawTorchProp(x + 64, fluidTop - 12, 0.95);","      drawSkullBanner(x, fluidTop - 88, 0.8, theme.amber);","    }","  }","","  // Hanging ceiling traps / decor like the references.","  const topBase = Math.floor(cameraX / 280) * 280;","  for (let i = -1; i < 7; i += 1) {","    const wx = topBase + i * 280;","    const x = worldToScreenX(wx + 180);","    if (x < -120 || x > VIEW_WIDTH + 120) continue;","    const seed = seeded(wx * 0.043);","    const topY = 52 + seed * 44;","    if (scene === 'arcane') {","      drawOrbStructure(x, topY + 34, 12 + seed*4, theme.cyan, theme.white || '#fff');","    } else {","      ctx.save();","      ctx.globalAlpha = 0.32;","      ctx.fillStyle = rgbaFromHex(theme.stoneA, 0.86);","      ctx.fillRect(x - 28,