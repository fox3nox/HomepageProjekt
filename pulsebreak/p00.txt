<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#050711">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="PULSEBREAK">
  <title>Pulsebreak — 1.2</title>
  <style>:root {
  color-scheme: dark;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: #050711;
  color: #f3fbff;
}

* { box-sizing: border-box; }
html, body { margin: 0; width: 100%; min-height: 100%; background: #050711; overflow: hidden; }
body { min-height: 100svh; }
button, canvas { -webkit-tap-highlight-color: transparent; }
button { font: inherit; }

.app-shell {
  min-height: 100svh;
  display: grid;
  place-items: center;
  padding: max(10px, env(safe-area-inset-top)) max(10px, env(safe-area-inset-right)) max(10px, env(safe-area-inset-bottom)) max(10px, env(safe-area-inset-left));
  background: radial-gradient(circle at 50% 42%, #111c35 0%, #050711 58%, #02040a 100%);
}

.game-frame {
  position: relative;
  width: min(100%, calc((100svh - 20px) * 16 / 9));
  aspect-ratio: 16 / 9;
  overflow: hidden;
  border: 1px solid rgba(83, 239, 255, 0.22);
  background: #050711;
  box-shadow: 0 0 0 1px rgba(167,121,255,.08), 0 30px 100px rgba(0,0,0,.58), 0 0 60px rgba(42,169,219,.08);
}

#game { display: block; width: 100%; height: 100%; touch-action: none; }

.top-ui {
  position: absolute;
  z-index: 5;
  inset: 14px 16px auto;
  display: grid;
  grid-template-columns: 180px minmax(220px, 480px) 44px;
  gap: 18px;
  align-items: start;
  pointer-events: none;
}

.brand-mini strong,
.brand-mini span { display: block; }
.brand-mini strong { font-size: 13px; letter-spacing: .22em; }
.brand-mini span { margin-top: 4px; font: 700 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .16em; color: rgba(226,241,255,.55); }

.progress-shell { width: 100%; }
.progress-track { height: 5px; overflow: hidden; background: rgba(184,215,240,.11); border: 1px solid rgba(83,239,255,.12); }
.progress-fill { width: 0%; height: 100%; background: linear-gradient(90deg, #53efff, #a679ff, #ff3e96); box-shadow: 0 0 18px rgba(83,239,255,.55); transition: width 80ms linear; }
.progress-meta { margin-top: 6px; display: flex; justify-content: space-between; gap: 12px; font: 700 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .1em; color: rgba(226,241,255,.52); }
.progress-meta strong { color: #f3fbff; font-size: 11px; }

.icon-button {
  pointer-events: auto;
  width: 42px;
  height: 42px;
  border: 1px solid rgba(83,239,255,.22);
  background: rgba(5,7,17,.62);
  color: #f3fbff;
  font-weight: 800;
  cursor: pointer;
  backdrop-filter: blur(10px);
}
.icon-button:disabled { opacity: .25; cursor: default; }

.hud {
  position: absolute;
  inset: auto 0 0 0;
  z-index: 4;
  pointer-events: none;
  display: flex;
  justify-content: space-between;
  padding: 0 18px 12px;
  font-size: 8px;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(226,241,255,.34);
}
.hud span { display: block; }
.hud-right { text-align: right; }

.overlay-panel {
  position: absolute;
  z-index: 10;
  inset: 0;
  display: grid;
  place-items: center;
  padding: 24px;
  background: radial-gradient(circle at 50% 42%, rgba(15,26,52,.55), rgba(3,5,12,.82));
  backdrop-filter: blur(8px);
}
.overlay-panel.hidden { display: none; }

.menu-card {
  width: min(500px, 92%);
  padding: 34px;
  border: 1px solid rgba(83,239,255,.24);
  background: linear-gradient(180deg, rgba(14,21,39,.94), rgba(7,10,20,.94));
  box-shadow: 0 28px 80px rgba(0,0,0,.48), inset 0 1px rgba(255,255,255,.04), 0 0 42px rgba(83,239,255,.06);
}

.signal-mark { display: flex; gap: 5px; height: 18px; align-items: end; margin-bottom: 20px; }
.signal-mark span { width: 26px; height: 3px; background: #53efff; box-shadow: 0 0 12px rgba(83,239,255,.65); }
.signal-mark span:nth-child(2) { height: 9px; background: #a679ff; }
.signal-mark span:nth-child(3) { height: 15px; background: #ff3e96; }

h1, h2, p { margin-top: 0; }
h1 { margin-bottom: 7px; font-size: clamp(34px, 6vw, 62px); line-height: .92; letter-spacing: .04em; }
h2 { margin-bottom: 14px; font-size: 42px; line-height: 1; }
.level-name, .eyeline { font: 800 10px/1.3 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .16em; color: #53efff; }
.menu-copy { margin: 18px 0 24px; max-width: 390px; color: rgba(226,241,255,.6); font-size: 13px; line-height: 1.55; }

.menu-actions { display: grid; gap: 10px; }
.primary-button,
.secondary-button,
.text-button {
  min-height: 56px;
  border: 0;
  cursor: pointer;
  text-align: left;
}
.primary-button,
.secondary-button { padding: 13px 16px; }
.primary-button { color: #041018; background: #53efff; box-shadow: 0 0 24px rgba(83,239,255,.2); }
.secondary-button { color: #f3fbff; background: #171c32; border: 1px solid rgba(167,121,255,.25); }
.primary-button strong,
.secondary-button strong { display: block; font-size: 13px; letter-spacing: .08em; }
.primary-button span,
.secondary-button span { display: block; margin-top: 4px; font-size: 10px; opacity: .62; }
.primary-button:active,
.secondary-button:active,
.text-button:active,
.icon-button:active { transform: translateY(1px); }

.menu-footer { margin-top: 18px; display: flex; align-items: center; justify-content: space-between; gap: 16px; font: 800 10px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .1em; color: rgba(226,241,255,.52); }
.text-button { min-height: 36px; padding: 8px 0; color: #53efff; background: transparent; text-align: right; font: 800 10px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .1em; }
.text-button.wide { text-align: center; width: 100%; }
.compact-card { width: min(390px, 92%); text-align: center; }
.compact-card .menu-actions { margin-top: 24px; }
.compact-card .primary-button,
.compact-card .secondary-button { text-align: center; }
.success-card { border-color: rgba(124,255,178,.3); box-shadow: 0 28px 80px rgba(0,0,0,.48), 0 0 44px rgba(124,255,178,.08); }
.success-card .eyeline { color: #7cffb2; }

@media (max-width: 700px) {
  .app-shell { padding: 0; }
  .game-frame { width: 100vw; max-height: 100svh; border: 0; }
  .top-ui { inset: 8px 9px auto; grid-template-columns: 88px 1fr 36px; gap: 8px; }
  .brand-mini strong { font-size: 9px; letter-spacing: .12em; }
  .brand-mini span { font-size: 7px; }
  .icon-button { width: 34px; height: 34px; }
  .progress-meta { font-size: 7px; }
  .progress-meta strong { font-size: 9px; }
  .hud { display: none; }
  .overlay-panel { padding: 12px; }
  .menu-card { width: min(430px, 96%); padding: 22px; }
  h1 { font-size: clamp(32px, 10vw, 52px); }
  .menu-copy { margin: 12px 0 18px; font-size: 11px; }
  .primary-button, .secondary-button { min-height: 48px; padding: 10px 13px; }
}

/* Phase 8 campaign / level selection */
.campaign-card { width: min(720px, 94%); padding: 26px 28px; }
