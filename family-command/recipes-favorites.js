/* Familienzentrale V9.55 · Rezepte & Favoriten · additive feature module */
(()=>{
'use strict';
if(window.__fcRecipesInstalled)return;window.__fcRecipesInstalled=true;
const VERSION='9.55.0',CATS=['Lebensmittel','Getränke','Haushalt','Drogerie','Schule','Sonstiges'];
const D=()=>{try{return typeof data!=='undefined'&&data?data:{}}catch(_){return{}}};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const uid=p=>`${p}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`;
const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
function persist(){try{if(typeof save==='function')save()}catch(e){console.error('fc_recipes_save',e)}try{window.__fcV9?.invalidate?.(['more'])}catch(_){}}
function store(create=true){const d=D();if(!d.recipes&&create)d.recipes={version:1,items:{}};if(d.recipes&&(!d.recipes.items||typeof d.recipes.items!=='object'||Array.isArray(d.recipes.items)))d.recipes.items={};return d.recipes||null}
function recipes(){return Object.values(store(false)?.items||{}).filter(x=>x&&!x.archived).sort((a,b)=>Number(!!b.favorite)-Number(!!a.favorite)||String(a.title||'').localeCompare(String(b.title||''),'de'))}
function favorites(){return recipes().filter(x=>x.favorite)}
function ingredients(x){return Array.isArray(x?.ingredients)?x.ingredients.filter(i=>i&&String(i.name||'').trim()):[]}
let filter='all',query='';
function visibleRecipes(){const q=query.trim().toLowerCase();return recipes().filter(x=>(filter!=='favorites'||x.favorite)&&(!q||`${x.title||''} ${x.note||''} ${x.instructions||''} ${ingredients(x).map(i=>i.name).join(' ')}`.toLowerCase().includes(q)))}
function tile(){const root=document.getElementById('more');if(!root)return;const existing=root.querySelector('[data-fc-recipes]'),txt=`${recipes().length} Rezept${recipes().length===1?'':'e'}${favorites().length?` · ${favorites().length} ★`:''}`;if(existing){existing.querySelector('span').textContent=txt;document.documentElement.dataset.fcRecipes='v55';return}const grid=root.querySelector('.fc9-more-grid');if(!grid)return;const b=document.createElement('button');b.type='button';b.className='fc9-tile';b.dataset.fcRecipes='1';b.innerHTML=`<i aria-hidden="true">${fcIcon('book-open')}</i><b>Rezepte & Favoriten</b><span>${txt}</span>`;b.onclick=open;const meals=grid.querySelector('[data-fc-meals]');if(meals)meals.after(b);else grid.appendChild(b);document.documentElement.dataset.fcRecipes='v55'}
function close(){document.getElementById('fcRecipesModal')?.remove();document.getElementById('fcRecipeEdit')?.remove();document.getElementById('fcRecipePlan')?.remove()}
function shell(){close();filter='all';query='';const m=document.createElement('div');m.id='fcRecipesModal';m.className='fc-recipes-modal';m.innerHTML=`<section class="fc-recipes-shell" role="dialog" aria-modal="true" aria-label="Rezepte und Favoriten"><header class="fc-recipes-head"><div><small>FAMILY COMMAND</small><h2>Rezepte & Favoriten</h2><p>Lieblingsgerichte speichern und direkt weiterplanen.</p></div><button type="button" class="fc-recipes-close" aria-label="Schließen">×</button></header><main class="fc-recipes-main"><div class="fc-recipes-toolbar"><label class="fc-recipes-search"><span>⌕</span><input type="search" placeholder="Rezepte durchsuchen" aria-label="Rezepte durchsuchen"></label><button type="button" class="fc-recipes-new">＋ Neues Rezept</button></div><div class="fc-recipes-filters"><button type="button" data-filter="all" class="active">Alle</button><button type="button" data-filter="favorites">★ Favoriten</button></div><div class="fc-recipes-grid"></div></main></section>`;m.querySelector('.fc-recipes-close').onclick=close;m.onclick=e=>{if(e.target===m)close()};m.querySelector('.fc-recipes-new').onclick=()=>editRecipe();m.querySelector('.fc-recipes-search input').oninput=e=>{query=e.target.value;render()};m.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;render()});document.body.appendChild(m);render();return m}
function recipeCard(x){const meta=[x.prepMinutes?`${Number(x.prepMinutes)} Min.`:'',x.servings?`${Number(x.servings)} Port.`:'',`${ingredients(x).length} Zutaten`].filter(Boolean).join(' · ');return `<article class="fc-recipe-card" data-recipe="${esc(x.id)}"><button type="button" class="fc-recipe-fav ${x.favorite?'active':''}" aria-label="${x.favorite?'Favorit entfernen':'Als Favorit markieren'}">★</button><button type="button" class="fc-recipe-open"><div class="fc-recipe-symbol">${x.favorite?'★':'🍴'}</div><div><h3>${esc(x.title)}</h3><p>${esc(meta)}</p>${x.note?`<span>${esc(x.note)}</span>`:''}</div><i>›</i></button></article>`}
function render(){const m=document.getElementById('fcRecipesModal');if(!m)return;const grid=m.querySelector('.fc-recipes-grid'),rows=visibleRecipes();m.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',b.dataset.filter===filter));grid.innerHTML=rows.length?rows.map(recipeCard).join(''):`<div class="fc-recipes-empty"><strong>${query?'Kein Treffer':'Noch keine Rezepte'}</strong><span>${query?'Ändere den Suchbegriff.':'Lege dein erstes Familienrezept an.'}</span></div>`;grid.querySelectorAll('[data-recipe]').forEach(card=>{const id=card.dataset.recipe;card.querySelector('.fc-recipe-open').onclick=()=>editRecipe(id);card.querySelector('.fc-recipe-fav').onclick=e=>{e.stopPropagation();toggleFavorite(id)}});tile()}
function ingredientRow(r={}){return `<div class="fc-recipe-ing" data-ing-row><input data-ing-name maxlength="70" placeholder="Zutat" value="${esc(r.name||'')}"><input data-ing-qty maxlength="24" placeholder="Menge" value="${esc(r.quantity||'')}"><select data-ing-cat>${CATS.map(c=>`<option ${c===(r.category||'Lebensmittel')?'selected':''}>${c}</option>`).join('')}</select><button type="button" data-remove-ing aria-label="Zutat entfernen">×</button></div>`}
function bindIngredientRemovers(root){root.querySelectorAll('[data-remove-ing]').forEach(b=>b.onclick=()=>b.closest('[data-ing-row]')?.remove())}
function readIngredients(root){return [...root.querySelectorAll('[data-ing-row]')].map(r=>({name:r.querySelector('[data-ing-name]').value.trim(),quantity:r.querySelector('[data-ing-qty]').value.trim(),category:r.querySelector('[data-ing-cat]').value})).filter(r=>r.name)}
function editRecipe(id=''){const s=store(),existing=id?s.items?.[id]:null,x=existing||{title:'',note:'',instructions:'',servings:4,prepMinutes:'',favorite:false,ingredients:[]};document.getElementById('fcRecipeEdit')?.remove();const e=document.createElement('div');e.id='fcRecipeEdit';e.className='fc-recipe-edit-modal';e.innerHTML=`<section class="fc-recipe-edit-sheet"><header><div><small>${existing?'REZEPT':'NEUES REZEPT'}</small><h3>${existing?esc(x.title):'Rezept anlegen'}</h3></div><button type="button" data-close>×</button></header><div class="fc-recipe-form"><label>NAME<input id="fcRecipeTitle" maxlength="90" value="${esc(x.title||'')}" placeholder="z. B. Spaghetti Carbonara"></label><div class="fc-recipe-form-grid"><label>PORTIONEN<input id="fcRecipeServings" type="number" min="1" max="30" value="${Number(x.servings)||4}"></label><label>DAUER (MIN.)<input id="fcRecipeMinutes" type="number" min="0" max="600" value="${x.prepMinutes?Number(x.prepMinutes):''}"></label></div><label>KURZNOTIZ<input id="fcRecipeNote" maxlength="140" value="${esc(x.note||'')}" placeholder="Optional"></label><div class="fc-recipe-ing-head"><b>Zutaten</b><span>${ingredients(x).length||'Optional'}</span></div><div id="fcRecipeIngredients">${ingredients(x).map(ingredientRow).join('')}</div><button type="button" class="fc-recipe-add-ing" data-add-ing>＋ Zutat</button><label>ZUBEREITUNG<textarea id="fcRecipeInstructions" maxlength="3000" placeholder="Schritte oder Hinweise">${esc(x.instructions||'')}</textarea></label></div><footer>${existing?'<button type="button" class="danger" data-delete>Löschen</button>':'<span></span>'}${existing?'<button type="button" data-favorite>★ '+(x.favorite?'Favorit entfernen':'Als Favorit')+'</button>':''}${existing?'<button type="button" data-shopping>🛒 Einkauf</button>':''}${existing?'<button type="button" data-plan>📅 Wochenplan</button>':''}<button type="button" data-cancel>Abbrechen</button><button type="button" class="primary" data-save>Speichern</button></footer></section>`;const done=()=>e.remove();e.querySelector('[data-close]').onclick=done;e.querySelector('[data-cancel]').onclick=done;e.onclick=v=>{if(v.target===e)done()};e.querySelector('[data-add-ing]').onclick=()=>{e.querySelector('#fcRecipeIngredients').insertAdjacentHTML('beforeend',ingredientRow());bindIngredientRemovers(e)};bindIngredientRemovers(e);e.querySelector('[data-save]').onclick=()=>{const title=e.querySelector('#fcRecipeTitle').value.trim();if(!title)return;const now=new Date().toISOString(),row=existing||{id:uid('recipe'),createdAt:now,favorite:false};row.title=title;row.servings=Math.max(1,Number(e.querySelector('#fcRecipeServings').value)||1);row.prepMinutes=Math.max(0,Number(e.querySelector('#fcRecipeMinutes').value)||0);row.note=e.querySelector('#fcRecipeNote').value.trim();row.instructions=e.querySelector('#fcRecipeInstructions').value.trim();row.ingredients=readIngredients(e);row.updatedAt=now;s.items[row.id]=row;persist();done();render()};e.querySelector('[data-delete]')?.addEventListener('click',()=>{if(!confirm('Rezept wirklich löschen?'))return;existing.archived=true;existing.updatedAt=new Date().toISOString();persist();done();render()});e.querySelector('[data-favorite]')?.addEventListener('click',()=>{existing.favorite=!existing.favorite;existing.updatedAt=new Date().toISOString();persist();done();render()});e.querySelector('[data-shopping]')?.addEventListener('click',()=>{const out=window.__fcMealPlanner?.addToShopping?.(existing,{source:'recipe'});if(out){announce(`${out.added} Zutat${out.added===1?'':'en'} hinzugefügt${out.skipped?` · ${out.skipped} bereits vorhanden`:''}`);done();render()}});e.querySelector('[data-plan]')?.addEventListener('click',()=>{done();planRecipe(existing)});document.body.appendChild(e);setTimeout(()=>{if(e.isConnected&&!e.contains(document.activeElement))e.querySelector('#fcRecipeTitle')?.focus()},30)}
function toggleFavorite(id){const x=store(false)?.items?.[id];if(!x)return;x.favorite=!x.favorite;x.updatedAt=new Date().toISOString();persist();render()}
function planRecipe(recipe){document.getElementById('fcRecipePlan')?.remove();const p=document.createElement('div');p.id='fcRecipePlan';p.className='fc-recipe-plan-modal';p.innerHTML=`<section class="fc-recipe-plan-sheet"><header><div><small>IN WOCHENPLAN</small><h3>${esc(recipe.title)}</h3></div><button type="button" data-close>×</button></header><div class="fc-recipe-plan-form"><label>TAG<input id="fcRecipePlanDate" type="date" value="${iso(new Date())}"></label><label>MAHLZEIT<select id="fcRecipePlanType"><option value="breakfast">Frühstück</option><option value="lunch">Mittagessen</option><option value="dinner" selected>Abendessen</option></select></label></div><footer><button type="button" data-cancel>Abbrechen</button><button type="button" class="primary" data-confirm>Einplanen</button></footer></section>`;const done=()=>p.remove();p.querySelector('[data-close]').onclick=done;p.querySelector('[data-cancel]').onclick=done;p.onclick=e=>{if(e.target===p)done()};p.querySelector('[data-confirm]').onclick=()=>{const date=p.querySelector('#fcRecipePlanDate').value,type=p.querySelector('#fcRecipePlanType').value;if(!date)return;let out=window.__fcMealPlanner?.planRecipe?.(date,type,recipe,false);if(out?.conflict){if(!confirm(`${out.title} ist dort bereits geplant. Ersetzen?`))return;out=window.__fcMealPlanner?.planRecipe?.(date,type,recipe,true)}if(out?.ok){announce('Rezept im Wochenplan eingetragen.');done();render()}};document.body.appendChild(p)}
function announce(msg){try{if(typeof toast==='function')toast(msg);else{const t=document.getElementById('toast');if(t){t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}}}catch(_){}}
function open(){shell()}
function install(){const root=document.getElementById('more');if(!root){setTimeout(install,60);return}const obs=new MutationObserver(()=>{clearTimeout(obs._t);obs._t=setTimeout(tile,25)});obs.observe(root,{childList:true,subtree:true});tile();document.querySelector('.fc9-nav button[data-screen="more"]')?.addEventListener('click',()=>setTimeout(tile,30));document.documentElement.dataset.fcRecipes='v55'}
window.fcOpenRecipes=open;window.__fcRecipes={version:VERSION,open,render,store,health:()=>({version:VERSION,recipes:recipes().length,favorites:favorites().length,mealPlannerIntegration:!!window.__fcMealPlanner,shoppingIntegration:!!window.__fcShoppingLists,cloudCompatible:true,objectMergeModel:true})};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();