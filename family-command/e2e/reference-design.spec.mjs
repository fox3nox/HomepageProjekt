import { webkit } from 'playwright';import { spawn } from 'node:child_process';import { readFileSync } from 'node:fs';import assert from 'node:assert/strict';
const PORT=4189,BASE=`http://127.0.0.1:${PORT}`,seed=readFileSync('family-command/e2e/mock-private-core.js','utf8'),server=spawn('python3',['-m','http.server',String(PORT),'--directory','family-command'],{stdio:['ignore','pipe','pipe']});const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function ready(){for(let i=0;i<60;i++){try{if((await fetch(BASE+'/index.html')).ok)return}catch{}await sleep(100)}throw Error('server not ready')}
try{await ready();const browser=await webkit.launch({headless:true}),ctx=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true,serviceWorkers:'block'});await ctx.addInitScript({content:seed});const page=await ctx.newPage();await page.goto(BASE+'/?access=test',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.fcReady==='1'&&window.__fcV9);await page.evaluate(()=>{const iso=todayISO();const d=new Date(`${iso}T12:00:00`);d.setDate(d.getDate()+1);const tomorrow=d.toISOString().slice(0,10);data.todos=[{id:'gift',date:iso,title:'Pokémon-Karten als Geschenk kaufen',priority:true,done:false,section:'day'},{id:'shop',date:iso,title:'Maggi kaufen',priority:true,done:false,section:'day'},{id:'normal',date:iso,title:'Normale offene Aufgabe',priority:false,done:false,section:'day'}];data.events=[{id:'visit',personIds:['child-a'],title:'Schulbesuch',date:iso,time:'13:40',note:'PET-Flaschen mitnehmen'},{id:'birthday',personIds:['child-a'],title:'Geburtstag Alessio',date:tomorrow,time:'13:30'}];for(const p of(data.people||[]).filter(p=>p.id!=='oli')){data.schedules[p.id]??={};for(const wd of[1,2,3,4,5])data.schedules[p.id][wd]=[{start:wd===3?'07:30':'08:20',end:'11:55',label:'Schule / Kindergarten',note:wd===3?'Turnzeug einpacken':''}]}save?.();window.renderToday?.()});await page.waitForFunction(()=>document.documentElement.dataset.fcReferenceDashboard==='v38');const r=await page.evaluate(()=>{const before=JSON.stringify({todos:data.todos,events:data.events,homework:data.homework,people:data.people,schedules:data.schedules});window.__fcReferenceDashboard38.rebuild(true);const after=JSON.stringify({todos:data.todos,events:data.events,homework:data.homework,people:data.people,schedules:data.schedules}),dash=document.querySelector('#today>.fc38-dashboard'),source=document.querySelector('#today>.fc9-page'),tasks=[...dash.querySelectorAll('.fc38-task')],school=dash.querySelector('.fc38-schoolgrid');return{same:before===after,version:window.__fcReferenceDashboard38.version,dashboard:!!dash,sourceWidth:source.getBoundingClientRect().width,overflow:document.documentElement.scrollWidth<=document.documentElement.clientWidth+1,daybar:dash.querySelector('.fc38-daybar').getBoundingClientRect().height,panels:dash.querySelectorAll('.fc38-panel').length,tasks:tasks.length,heights:tasks.map(x=>x.getBoundingClientRect().height),lines:dash.querySelectorAll('.fc38-line').length,schoolCells:school.children.length,schoolWidth:school.getBoundingClientRect().width,viewport:innerWidth,reminders:dash.querySelectorAll('.fc38-reminders>*').length,text:dash.textContent,nav:document.querySelector('.fc9-nav').getBoundingClientRect().height}});console.log('reference-v38',JSON.stringify(r));assert.equal(r.version,'9.38.0');assert.ok(r.dashboard&&r.same&&r.overflow);assert.ok(r.sourceWidth<=1.5);assert.ok(r.daybar>=38&&r.daybar<=48);assert.equal(r.tasks,3);for(const h of r.heights)assert.ok(h>=36&&h<=50);assert.ok(r.lines>=2);assert.ok(r.panels>=4);assert.ok(r.schoolCells>=24,'full Mon-Fri school grid must exist');assert.ok(r.schoolWidth<=r.viewport,'full school week must fit iPhone width');assert.equal(r.reminders,2);for(const x of['Das Wichtigste zuerst','im Überblick','Vorschau','SCHULZEITEN & WICHTIGES','NIE VERGESSEN','WICHTIG FÜR ELIYAH','Leuchtweste','Turnschuhe'])assert.ok(r.text.includes(x),x);assert.ok(r.nav>45);await browser.close();console.log('V9.38 reference fidelity: ok')}finally{server.kill('SIGTERM')}