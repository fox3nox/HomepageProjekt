s="modalActions"><button class="btn" data-close="ruleModal">Abbrechen</button><button class="btn primary" id="saveRule">Speichern</button></div></div></div>
<div class="modalBg" id="personModal"><div class="modal"><div class="modalHead"><h3>Person hinzufügen</h3><button class="moreBtn" data-close="personModal">✕</button></div><div class="field"><label>Name</label><input id="personName"></div><div class="modalActions"><button class="btn" data-close="personModal">Abbrechen</button><button class="btn primary" id="savePerson">Speichern</button></div></div></div>
<div class="modalBg" id="accountModal"><div class="modal" style="max-width:440px"><div class="modalHead"><div><h3>Family Command Konto</h3><div class="tiny">Ein Konto für alle deine Geräte</div></div><button class="moreBtn" data-close="accountModal">✕</button></div><div id="authSignedOut"><div class="authSwitch"><button class="active" id="authLoginTab" type="button">Anmelden</button><button id="authSignupTab" type="button">Konto erstellen</button></div><div class="authError" id="authError"></div><div class="form"><div class="field full"><label>E-Mail</label><input id="authEmail" type="email" autocomplete="email" placeholder="name@beispiel.ch"></div><div class="field full"><label>Passwort</label><input id="authPassword" type="password" autocomplete="current-password" minlength="8" placeholder="Mindestens 8 Zeichen"></div></div><div class="authHint" id="authHint">Melde dich mit demselben Konto auf weiteren Geräten an. Deine Planungsdaten werden dann automatisch synchronisiert.</div><div class="modalActions"><button class="btn" id="resetPasswordBtn" type="button">Passwort vergessen</button><button class="btn primary" id="authSubmit" type="button">Anmelden</button></div></div><div id="authSignedIn" class="hidden"><div class="accountSummary"><div class="accountAvatar" id="accountAvatar">F</div><div><b id="accountEmail">—</b><span>Cloud-Synchronisation aktiv</span></div></div><div class="scanCard"><strong>Mehrere Geräte</strong><p>Melde dich auf einem zweiten iPhone, iPad oder Computer mit derselben E-Mail an. Änderungen erscheinen automatisch auf beiden Geräten.</p></div><div class="modalActions"><button class="btn" id="accountSyncBtn" type="button">Jetzt synchronisieren</button><button class="btn danger" id="accountSignOutBtn" type="button">Abmelden</button></div></div></div></div><div class="modalBg" id="scanModal"><div class="modal" style="max-width:760px"><div class="modalHead"><div><h3>Dokument verstehen</h3><div class="tiny">Erkennen → prüfen → mehrere Aktionen übernehmen</div></div><button class="moreBtn" data-close="scanModal">✕</button></div><div id="scanBusy" class="scanStatus"><div class="spinner"></div><div style="flex:1"><b style="font-size:12px" id="scanStatusText">Text wird gelesen …</b><div class="progress"><i id="scanProgress"></i></div></div></div><div id="scanReview" class="hidden"><div class="smartBanner"><div>✦</div><div><strong>Family Command hat den Zettel zerlegt.</strong><p>Nichts wird automatisch gespeichert. Prüfe die erkannten Angaben und wähle aus, welche Aktionen wirklich angelegt werden sollen.</p></div></div><div class="smartFacts" id="smartFacts"></div><div class="confidence" id="scanConfidence"></div><div class="smartSection"><div class="smartSectionHead"><h4>Vorgeschlagene Aktionen</h4><span id="proposalCount">0 ausgewählt</span></div><div class="proposalList" id="proposalList"></div></div><details class="rawDetails"><summary>Erkannten Originaltext anzeigen / korrigieren</summary><textarea class="rawBox" id="scanRaw"></textarea><div style="display:flex;justify-content:flex-end;margin-top:7px"><button class="btn small" id="reanalyzeText">Korrigierten Text neu auswerten</button></div></details><div class="modalActions"><button class="btn" data-close="scanModal">Abbrechen</button><button class="btn primary" id="saveScan">Ausgewählte übernehmen</button></div></div></div></div><div class="toast" id="toast">Gespeichert</div>
<script>
const KEY='family-command-pro-v6',LEGACY_KEY='family-command-pro-v2',COLORS=['#5b52f2','#19815f','#cf6b4b','#a55bc5','#2e7cc6','#bf8a24'];const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const nowDay=()=>{const d=new Date();d.setHours(0,0,0,0);return d},iso=d=>{const x=new Date(d);x.setMinutes(x.getMinutes()-x.getTimezoneOffset());return x.toISOString().slice(0,10)},addDays=(d,n)=>{const x=new Date(d);x.setDate(x.getDate()+n);return x},uid=()=>Date.now()+Math.floor(Math.random()*9999),esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),tmin=t=>{const[a,b]=t.split(':').map(Number);return a*60+b},mtime=m=>{m=(m+1440)%1440;return String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0')};

const SUPABASE_URL=window.FC_CLOUD?.url||'',SUPABASE_KEY=window.FC_CLOUD?.key||'';
const DEVICE_KEY='family-command-device-v6';let deviceId=localStorage.getItem(DEVICE_KEY);if(!deviceId){deviceId=(crypto.randomUUID?crypto.randomUUID():'dev-'+Date.now()+'-'+Math.random().toString(36).slice(2));localStorage.setItem(DEVICE_KEY,deviceId)}
let sb=null,currentUser=null,realtimeChannel=null,syncTimer=null,cloudReady=false,applyingRemote=false,lastRemoteUpdatedAt='';
function cloudConfigured(){return SUPABASE_URL.startsWith('https://')&&SUPABASE_KEY.startsWith('sb_')&&window.supabase?.createClient}
function setSyncState(state,label){const b=$('#syncBadge'),l=$('#syncLabel');if(!b||!l)return;b.classList.remove('online','busy','offline');if(state)b.classList.add(state);l.textContent=label}
function logSync(text){const el=$('#syncLog');if(el)el.textContent=text}
function cloudErrorMessage(e){return e?.message||String(e||'Unbekannter Fehler')}
function renderAccountState(){const signed=!!currentUser,configured=cloudConfigured();if($('#authSignedOut'))$('#authSignedOut').classList.toggle('hidden',signed);if($('#authSignedIn'))$('#authSignedIn').classList.toggle('hidden',!signed);if($('#accountEmail'))$('#accountEmail').textContent=currentUser?.email||'—';if($('#accountAvatar'))$('#accountAvatar').textContent=(currentUser?.email||'F')[0].toUpperCase();if($('#cloudAccountTitle'))$('#cloudAccountTitle').textContent=signed?currentUser.email:configured?'Noch nicht angemeldet':'Cloud vorbereitet';if($('#cloudAccountText'))$('#cloudAccountText').textContent=signed?'Dieses Konto synchronisiert Family Command auf mehreren Geräten.':configured?'Daten bleiben bis zur Anmeldung nur auf diesem Gerät.':'Für die Aktivierung braucht Family Command ein eigenes Supabase-Projekt.';if($('#accountBtn'))$('#accountBtn').textContent=signed?'Konto anzeigen':configured?'Anmelden':'Cloud-Status';if($('#syncNowBtn'))$('#syncNowBtn').classList.toggle('hidden',!signed);if($('#signOutBtn'))$('#signOutBtn').classList.toggle('hidden',!signed);if($('#deviceTag'))$('#deviceTag').textContent='Gerät '+deviceId.slice(0,5);if($('#cloudSyncText'))$('#cloudSyncText').textContent=signed?(navigator.onLine?'Cloud-Synchronisation aktiv':'Offline · Änderungen werden vorgemerkt'):configured?'Bereit zur Anmeldung':'Nur lokale Speicherung';setSyncState(signed?(navigator.onLine?'online':'offline'):'',signed?'Synchronisiert':'Lokal')}
async function initCloud(){if(!cloudConfigured()){renderAccountState();logSync('Cloud ist in dieser Version vorbereitet, aber noch nicht mit einem Backend verbunden.');return}sb=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});const {data:{session}}=await sb.auth.getSession();currentUser=session?.user||null;cloudReady=true;renderAccountState();if(currentUser)await startCloudSession();sb.auth.onAuthStateChange(async(event,session)=>{const next=session?.user||null;if(next?.id===currentUser?.id&&event==='TOKEN_REFRESHED')return;currentUser=next;renderAccountState();if(currentUser)await startCloudSession();else stopRealtime()})}
function stopRealtime(){if(realtimeChannel&&sb){sb.removeChannel(realtimeChannel).catch(()=>{});realtimeChannel=null}}
async function startCloudSession(){if(!sb||!currentUser)return;setSyncState('busy','Synchronisiere…');await pullOrSeedCloud();subscribeRealtime();renderAccountState()}
function applyRemoteState(remote,updatedAt){applyingRemote=true;data=remote;data.settings={prep:55,travel:15,pickup:15,evening:'19:00',...(data.settings||{})};data.meta={...(data.meta||{}),dirty:false,cloudUserId:currentUser?.id||'',lastSyncedAt:updatedAt||new Date().toISOString(),updatedAt:data.meta?.updatedAt||updatedAt||new Date().toISOString()};localStorage.setItem(KEY,JSON.stringify(data));applyingRemote=false;renderAll()}
async function pullOrSeedCloud(){if(!sb||!currentUser)return;try{const {data:row,error}=await sb.from('user_state').select('state,updated_at,device_id').eq('user_id',currentUser.id).maybeSingle();if(error)throw error;if(!row){await pushCloud(true);return}lastRemoteUpdatedAt=row.updated_at||'';const remote=row.state;if(!remote){await pushCloud(true);return}const sam