eCloud=data.meta?.cloudUserId===currentUser.id,lastSynced=Date.parse(data.meta?.lastSyncedAt||0)||0,remoteUpdated=Date.parse(row.updated_at||0)||0,dirty=!!data.meta?.dirty;if(sameCloud&&dirty&&lastSynced&&remoteUpdated>lastSynced+1000){try{localStorage.setItem('family-command-conflict-'+Date.now(),JSON.stringify(data))}catch{}applyRemoteState(remote,row.updated_at);toast('Cloud-Konflikt: lokale Version gesichert')}else if(sameCloud&&dirty){await pushCloud(true)}else{applyRemoteState(remote,row.updated_at);if(LOCAL_SOURCE!=='seed')toast('Cloud-Daten geladen')}setSyncState('online','Synchronisiert');logSync('Letzte Synchronisation: '+new Intl.DateTimeFormat('de-CH',{hour:'2-digit',minute:'2-digit'}).format(new Date()))}catch(e){setSyncState('offline','Sync-Fehler');logSync('Synchronisation fehlgeschlagen: '+cloudErrorMessage(e))}}
function queueCloudSync(){if(applyingRemote||!currentUser||!sb)return;clearTimeout(syncTimer);setSyncState('busy','Speichert…');syncTimer=setTimeout(()=>pushCloud(false),650)}
async function pushCloud(force=false){if(!sb||!currentUser)return false;if(!navigator.onLine&&!force){setSyncState('offline','Offline');logSync('Offline gespeichert. Wird automatisch synchronisiert, sobald wieder Internet da ist.');return false}try{setSyncState('busy','Speichert…');const stateForCloud=typeof structuredClone==='function'?structuredClone(data):JSON.parse(JSON.stringify(data));stateForCloud.meta={...(stateForCloud.meta||{}),dirty:false,cloudUserId:currentUser.id};const payload={user_id:currentUser.id,state:stateForCloud,device_id:deviceId,updated_at:new Date().toISOString()};const {data:row,error}=await sb.from('user_state').upsert(payload,{onConflict:'user_id'}).select('updated_at').single();if(error)throw error;lastRemoteUpdatedAt=row?.updated_at||payload.updated_at;data.meta={...(data.meta||{}),dirty:false,cloudUserId:currentUser.id,lastSyncedAt:lastRemoteUpdatedAt};try{localStorage.setItem(KEY,JSON.stringify(data))}catch{}setSyncState('online','Synchronisiert');logSync('Gespeichert um '+new Intl.DateTimeFormat('de-CH',{hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date()));return true}catch(e){setSyncState('offline','Sync-Fehler');logSync('Cloud-Speicherung fehlgeschlagen: '+cloudErrorMessage(e));return false}}
function subscribeRealtime(){if(!sb||!currentUser)return;stopRealtime();realtimeChannel=sb.channel('family-command-'+currentUser.id).on('postgres_changes',{event:'UPDATE',schema:'public',table:'user_state',filter:'user_id=eq.'+currentUser.id},payload=>{const row=payload.new;if(!row||row.device_id===deviceId||row.updated_at===lastRemoteUpdatedAt)return;lastRemoteUpdatedAt=row.updated_at||'';if(row.state){if(data.meta?.dirty){try{localStorage.setItem('family-command-conflict-'+Date.now(),JSON.stringify(data))}catch{}}applyRemoteState(row.state,row.updated_at);setSyncState('online','Aktualisiert');toast('Auf anderem Gerät geändert')}}).subscribe()}
window.addEventListener('online',()=>{renderAccountState();if(currentUser)pullOrSeedCloud()});window.addEventListener('offline',()=>{renderAccountState();if(currentUser)setSyncState('offline','Offline')});

function seed(){const d=nowDay();return{people:[{id:'p1',name:'Kind 1',color:COLORS[0]},{id:'p2',name:'Kind 2',color:COLORS[1]},{id:'p3',name:'Kind 3',color:COLORS[2]}],settings:{prep:55,travel:15,pickup:15,evening:'19:00'},events:[{id:uid(),personId:'p1',title:'Schule',date:iso(d),time:'08:10',note:'Rucksack prüfen',reminder:30},{id:uid(),personId:'p2',title:'Schule',date:iso(d),time:'08:20',note:'Turnzeug mitnehmen',reminder:30},{id:uid(),personId:'p3',title:'Kindergarten',date:iso(d),time:'09:00',note:'Trinkflasche einpacken',reminder:15},{id:uid(),personId:'p3',title:'Abholen',date:iso(d),time:'11:45',note:'Kindergarten',reminder:15}],tasks:[{id:uid(),personId:'p1',title:'Rucksack kontrollieren',date:iso(d),note:'',done:false},{id:uid(),personId:'p2',title:'Hausaufgaben prüfen',date:iso(d),note:'',done:false},{id:uid(),personId:'p3',title:'Trinkflasche einpacken',date:iso(d),note:'',done:false}],rules:[{id:uid(),personId:'p1',title:'Schule',day:1,time:'08:10',end:'11:50',note:'Wochenplan'},{id:uid(),personId:'p2',title:'Schule',day:2,time:'08:20',end:'11:55',note:'Wochenplan'},{id:uid(),personId:'p3',title:'Kindergarten',day:3,time:'09:00',end:'11:45',note:'Wochenplan'}]}}const RAW_CURRENT=localStorage.getItem(KEY),RAW_LEGACY=localStorage.getItem(LEGACY_KEY),LOCAL_SOURCE=RAW_CURRENT?'current':RAW_LEGACY?'legacy':'seed';let data;try{data=JSON.parse(RAW_CURRENT||RAW_LEGACY)||seed()}catch{data=seed()}data.settings={prep:55,travel:15,pickup:15,evening:'19:00',...(data.settings||{})};data.meta={...(data.meta||{}),updatedAt:data.meta?.updatedAt||(LOCAL_SOURCE==='current'?new Date().toISOString():'1970-01-01T00:00:00.000Z'),dirty:!!data.meta?.dirty};let filter='all',view='today',editing=null,installPrompt=null;const localSave=(markDirty=true)=>{data.meta={...(data.meta||{}),updatedAt:new Date().toISOString(),dirty:markDirty?true:!!data.meta?.dirty};try{localStorage.setItem(KEY,JSON.stringify(data))}catch{}};const save=()=>{localSave(true);queueCloudSync()};const person=id=>data.people.find(p=>p.id===id)||{name:'Unbekannt',color:'#999'};const fmt=(ds,o={weekday:'long',day:'2-digit',month:'2-digit'})=>new Intl.DateTimeFormat('de-CH',o).format(new Date(ds+'T12:00:00'));function eventsOn(ds){const wd=new Date(ds+'T12:00:00').getDay();return [...data.events.filter(e=>e.date===ds),...data.rules.filter(r=>Number(r.day)===wd).map(r=>({id:'r'+r.id+ds,ruleId:r.id,personId:r.personId,title:r.title,date:ds,time:r.time,note:r.note,end:r.end||'',recurring:true,reminder:0}))].sort((a,b)=>a.time.localeCompare(b.time))}const filt=a=>filter==='all'?a:a.filter(x=>x.personId===filter);
function setView(v){view=v;$$('.view').forEach(x=>x.classList.toggle('active',x.id==='view-'+v));$$('[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===v));if(v==='plan')renderPlan();if(v==='week')renderWeek();if(v==='docs')renderDocs();if(v==='settings')renderSettings();window.scrollTo(0,0)}$$('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));$$('[data-go]').forEach(b=>b.onclick=()=>setView(b.dataset.go));
function renderFilters(){$('#filters').innerHTML=`<button class="chip ${filter==='all'?'active':''}" data-filter="all">Alle</button>`+data.people.map(p=>`<button class="chip ${filter===p.id?'active':''}" data-filter="${p.id}"><span class="dot" style="background:${p.color}"></span>${esc(p.name)}</button>`).join('');$$('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;renderAll()})}function syncSelects(){const o=data.people.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');$('#personInput').innerHTML=o;$('#rulePerson').innerHTML=o;if($('#scanPerson'))$('#scanPerson').innerHTML=o}
function atDateTime(ds,time){const d=new Date(ds+'T00:00:00');const[h,m]=String(time||'00:00').split(':').map(Number);d.setHours(h||0,m||0,0,0);return d}
function actionId(a){return [a.kind,a.date,a.time,a.eventId||'',a.personId||''].join('|')}
function buildSmartActions(base=new Date()){const today=iso(base),tomorrow=iso(addDays(nowDay(),1)),actions=[],travel=Number(data.settings.travel||0),prep=Number(data.settings.prep||0),pickup=Number(data.settings.pickup||travel||0),evening=data.settings.evening||'19:00';const add=(x)=>actions.push({...x,when:atDateTime(x.date,x.time)});const todayEvents=filt(eventsOn(today));if(todayEvents.length){const first=todayEvents[0],start=tmin(first.time),leave=mtime(start-travel),wake=mtime(start-travel-prep);add({kind:'wake',date:today,time:wake,label:'Wecker',meta:`Für ${first.title} · ${person(first.personId).name}`,personId:first.personId,eventId:first.id});add({kind:'leave',date:today,time:leave,label:'Losgehen',meta:`${first.title} um ${first.time}`,personId:first.personId,eventId:first.id});}todayEvents.forEach(e=>{if(!e.recurring&&Number(e.reminder||0)>0)add({kind:'event',date:today,time:mtime(tmin(e.time)-Number(e.reminder)),label:`${e.title} bald`,meta:`${e.reminder} Min. vorher · ${person(e.personId).name}`,personId:e.personId,eventId:e.id});if(e.end)add({kind:'pickup',date:today,time:mtime(tmin(e.end)-pickup),label:'Zum Abholen los',meta:`${e.title} endet ${e.end} · ${person(e.personId).name}`,personId:e.personId,eventId:e.id})});const tomorrowEvents=filt(eventsOn(tomorrow));if(tomorrowEvents.length){const first=tomorrowEvents[0];const prepTasks=filt(data.tasks.filter(t=>t.date===today&&!t.done));add({kind:'evening',date:today,time:evening,label:'Morgen vorbereiten',meta:`${first.time} ${first.title}${prepTasks.length?' · '+prepTasks.slice(0,2).map(t=>t.title).join(' / '):''}`,personId:first.personId,eventId:first.id})}return actions.sort((a,b)=>a.when-b.when)}
function renderSmartTimeline(){const list=buildSmartActions(),now=new Date(),soon=list.filter(a=>a.when.getTime()>now.getTime()-5*60000).slice(0,6),root=$('#smartTimeline');if(!root)return;if(!soon.length){root.innerHTML='<div class="smartEmpty