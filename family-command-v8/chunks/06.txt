">Keine automatischen Erinnerungen für heute.</div>';$('#nextActionText').textContent='Heute ist aus der aktuellen Planung nichts mehr offen.';return}root.innerHTML=soon.slice(0,4).map(a=>{const past=a.when<now,cls=a.kind==='pickup'?'warn':a.kind==='evening'?'good':'';return`<div class="smartItem ${cls} ${past?'past':''}"><div class="smartTime">${esc(a.time)}</div><div class="smartLabel">${esc(a.label)}</div><div class="smartMeta">${esc(a.meta)}</div></div>`}).join('');const next=soon.find(a=>a.when>=now)||soon[soon.length-1],mins=Math.max(0,Math.round((next.when-now)/60000));$('#nextActionTitle').textContent=next.label;$('#nextActionText').textContent=mins<1?'jetzt':mins<60?`in ${mins} Minuten`:`um ${next.time} · ${next.meta}`;$('#smartReminderState').textContent=('Notification'in window)&&Notification.permission==='granted'?'Benachrichtigungen aktiv':'Plan aktiv'}
function notificationLedger(){try{return JSON.parse(localStorage.getItem('fc-notified-v5')||'{}')}catch{return{}}}
function saveNotificationLedger(x){const cutoff=Date.now()-3*86400000;for(const k of Object.keys(x))if(Number(x[k])<cutoff)delete x[k];localStorage.setItem('fc-notified-v5',JSON.stringify(x))}
function sendNotificationForAction(a){if(!('Notification'in window)||Notification.permission!=='granted')return;const ledger=notificationLedger(),id=actionId(a);if(ledger[id])return;ledger[id]=Date.now();saveNotificationLedger(ledger);const body=a.meta||person(a.personId).name;try{new Notification(a.label,{body,tag:id})}catch{}}
function checkSmartNotifications(){const now=Date.now();buildSmartActions(new Date()).forEach(a=>{const diff=a.when.getTime()-now;if(diff<=30000&&diff>=-60000)sendNotificationForAction(a)});renderSmartTimeline()}
function renderToday(){const ds=iso(nowDay()),ev=filt(eventsOn(ds)),tasks=filt(data.tasks.filter(t=>t.date===ds));$('#todayLabel').textContent='Heute · '+fmt(ds);$('#agendaDate').textContent=fmt(ds,{day:'2-digit',month:'long'});$('#eventCount').textContent=ev.length;$('#openCount').textContent=tasks.filter(t=>!t.done).length;$('#personCount').textContent=filter==='all'?data.people.length:1;if(ev.length){const m=tmin(ev[0].time),travel=Number(data.settings.travel),prep=Number(data.settings.prep);$('#leaveTime').textContent=mtime(m-travel);$('#readyTime').textContent=mtime(m-travel);$('#wakeTime').textContent=mtime(m-travel-prep);$('#nextStep').textContent=`${ev[0].time} · ${ev[0].title} (${person(ev[0].personId).name})`;$('#morningStatus').textContent='Berechnet'}else{$('#leaveTime').textContent=$('#readyTime').textContent=$('#wakeTime').textContent='—';$('#nextStep').textContent='Heute nichts geplant.';$('#morningStatus').textContent='Freier Start'}$('#agenda').innerHTML=ev.length?ev.map(e=>`<div class="event"><div class="etime">${e.time}${e.end?`<span style="display:block;font-size:9px;color:var(--muted);font-weight:700">bis ${e.end}</span>`:''}</div><div class="eventBody" style="border-color:${person(e.personId).color}"><div class="eventTitle">${esc(e.title)}</div><div class="meta">${esc(person(e.personId).name)}${e.note?' · '+esc(e.note):''}${e.recurring?' · wiederkehrend':''}</div></div>${e.recurring?'':`<button class="moreBtn" data-edit-event="${e.id}">•••</button>`}</div>`).join(''):'<div class="empty">Keine Termine für diesen Filter.</div>';$('#tasks').innerHTML=tasks.length?tasks.map(t=>`<div class="task ${t.done?'done':''}" data-task="${t.id}"><div class="check">${t.done?'✓':''}</div><div class="taskBody"><div class="taskName">${esc(t.title)}</div><div class="meta">${esc(person(t.personId).name)}${t.note?' · '+esc(t.note):''}</div></div><button class="moreBtn" data-edit-task="${t.id}">•••</button></div>`).join(''):'<div class="empty">Keine Aufgaben.</div>';$$('[data-task]').forEach(x=>x.onclick=e=>{if(e.target.closest('[data-edit-task]'))return;const t=data.tasks.find(a=>String(a.id)===x.dataset.task);if(t){t.done=!t.done;save();renderToday()}});$$('[data-edit-event]').forEach(b=>b.onclick=()=>openEdit('event',b.dataset.editEvent));$$('[data-edit-task]').forEach(b=>b.onclick=()=>openEdit('task',b.dataset.editTask));const open=tasks.filter(t=>!t.done);$('#noticeText').textContent=open.length?open.slice(0,4).map(t=>t.title).join(' · '):'Alle heutigen Aufgaben sind erledigt.';$('#notice').style.background=open.length?'var(--warn2)':'var(--good2)';const days=[];let d=nowDay();while(days.length<5){if(![0,6].includes(d.getDay()))days.push(new Date(d));d.setDate(d.getDate()+1)}$('#days').innerHTML=days.map(d=>{const a=filt(eventsOn(iso(d)));return`<div class="day"><div class="dayTop"><b>${new Intl.DateTimeFormat('de-CH',{weekday:'short'}).format(d)}</b><span class="tiny">${new Intl.DateTimeFormat('de-CH',{day:'2-digit',month:'2-digit'}).format(d)}</span></div>${a.length?a.slice(0,3).map(e=>`<div class="mini"><b>${e.time}${e.end?`–${e.end}`:''}</b> ${esc(e.title)}<br>${esc(person(e.personId).name)}</div>`).join(''):'<div class="empty">frei</div>'}</div>`}).join('');renderSmartTimeline()}
function weekStart(){const d=nowDay(),x=d.getDay()||7;d.setDate(d.getDate()-x+1);return d}function renderWeek(){const s=weekStart(),days=Array.from({length:7},(_,i)=>addDays(s,i));$('#weekRange').textContent=`${new Intl.DateTimeFormat('de-CH',{day:'2-digit',month:'long'}).format(s)} – ${new Intl.DateTimeFormat('de-CH',{day:'2-digit',month:'long',year:'numeric'}).format(days[6])}`;$('#weekBoard').innerHTML=days.map(d=>{const ds=iso(d),ev=filt(eventsOn(ds)),ts=filt(data.tasks.filter(t=>t.date===ds));return`<div class="weekDay ${ds===iso(nowDay())?'today':''}"><div class="weekDayHead"><b>${new Intl.DateTimeFormat('de-CH',{weekday:'short'}).format(d)}</b><span class="tiny">${new Intl.DateTimeFormat('de-CH',{day:'2-digit',month:'2-digit'}).format(d)}</span></div>${ev.map(e=>`<div class="weekItem" style="border-color:${person(e.personId).color}"><b>${e.time} · ${esc(e.title)}</b>${esc(person(e.personId).name)}</div>`).join('')}${ts.map(t=>`<div class="weekTask">☐ ${esc(t.title)}<br><span class="tiny">${esc(person(t.personId).name)}</span></div>`).join('')}${!ev.length&&!ts.length?'<div class="empty">frei</div>':''}</div>`}).join('');$('#ruleList').innerHTML=data.rules.length?data.rules.map(r=>`<div class="rule"><div class="avatar" style="background:${person(r.personId).color}">${esc(person(r.personId).name[0])}</div><div class="ruleMain"><b>${esc(r.title)}</b><span>${['So','Mo','Di','Mi','Do','Fr','Sa'][r.day]} · ${r.time}–${r.end} · ${esc(person(r.personId).name)}</span></div><button class="moreBtn" data-del-rule="${r.id}">✕</button></div>`).join(''):'<div class="empty">Noch kein wiederkehrender Wochenplan.</div>';$$('[data-del-rule]').forEach(b=>b.onclick=()=>{data.rules=data.rules.filter(r=>String(r.id)!==b.dataset.delRule);save();renderWeek();renderPlan();renderToday();toast('Wochenplan gelöscht')})}
function renderPlan(){const wd=[1,2,3,4,5],names=['Montag','Dienstag','Mittwoch','Donnerstag','Freitag'],rules=filt(data.rules.filter(r=>wd.includes(Number(r.day)))).sort((a,b)=>a.time.localeCompare(b.time));const times=[...new Set(rules.map(r=>r.time))].sort();$('#planRuleCount').textContent=rules.length;$('#planPeopleCount').textContent=filter==='all'?data.people.length:1;let h='<div class="ttCell ttHead"></div>'+names.map(n=>`<div class="ttCell ttHead">${n}</div>`).join('');if(!times.length){$('#timetable').innerHTML=h+'<div class="planEmpty" style="grid-column:1/-1">Noch keine festen Zeiten. Mit „+ Stundenzeit“ kannst du den ersten Eintrag anlegen.</div>';return}for(const time of times){h+=`<div class="ttCell ttTime">${time}</div>`;for(const day of wd){const a=rules.filter(r=>Number(r.day)===day&&r.time===time);h+=`<div class="ttCell">${a.map(r=>`<div class="ttItem" style="border-color:${person(r.personId).color}"><b>${esc(r.title)} · ${r.time}–${r.end}</b><span>${esc(person(r.personId).name)}${r.note?' · '+esc(r.note):''}</span></div>`).join('')}</div>`}}$('#timetable').innerHTML=h}
function renderSettings(){$('#prepMinutes').value=data.settings.prep;$('#travelMinutes').value=data.settings.travel;$('#pickupMinutes').value=data.settings.pickup;$('#eveningHour').value=data.settings.evening||'19:00';$('#peopleSettings').innerHTML=data.people.map(p=>`<div class="personRow"><div class="avatar" style="background:${p.color}">${esc(p.name[0])}</div><input class="personInput" data-person="${p.id}" value="${esc(p.name)}"><button class="moreBtn" data-del-person="${p.id}" ${data.people.length===1?'disabled':''}>✕</button></div>`).join('');$$('[data-person]').forEach(i=>i.onchange=()=>{const p=person(i.dataset.person);if(i.value.trim()){p.name=i.value.trim();save();renderAll()}});$$('[data-del-person]').forEach(b=>b.onclick=()=>{if(data.people.length===1)return;const id=b.dataset.delPerson;if(!confirm(`${person(id).name} wirklich löschen?`))return;data.people=data.people.filter(p=>p.id!==id);data.events=data.events.filter(x=>x.personId!==id);data.tasks=data.tasks.filter(x=>x.personId!==id);data.rules=data.rules.filter(x=>x.personId!==id);if(filter===id)filter='