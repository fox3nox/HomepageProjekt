all';save();renderAll()});updateNotifications()}
function renderAll(){renderFilters();syncSelects();renderToday();if(view==='plan')renderPlan();if(view==='week')renderWeek();if(view==='settings')renderSettings()}function open(id){$('#'+id).classList.add('open')}function close(id){$('#'+id).classList.remove('open')}$$('[data-close]').forEach(b=>b.onclick=()=>close(b.dataset.close));$$('.modalBg').forEach(m=>m.onclick=e=>{if(e.target===m)close(m.id)});function typeFields(){$$('.eventOnly').forEach(x=>x.classList.toggle('hidden',$('#typeInput').value!=='event'))}$('#typeInput').onchange=typeFields;
function openNew(){editing=null;$('#entryTitle').textContent='Neuen Eintrag hinzufügen';$('#deleteEntry').classList.add('hidden');$('#titleInput').value='';$('#typeInput').value='event';$('#dateInput').value=iso(nowDay());$('#timeInput').value='08:00';$('#endTimeInput').value='';$('#noteInput').value='';$('#reminderInput').value='30';typeFields();open('entryModal')}$('#addBtn').onclick=openNew;function openEdit(type,id){const a=(type==='event'?data.events:data.tasks).find(x=>String(x.id)===String(id));if(!a)return;editing={type,id:a.id};$('#entryTitle').textContent='Eintrag bearbeiten';$('#deleteEntry').classList.remove('hidden');$('#titleInput').value=a.title;$('#personInput').value=a.personId;$('#typeInput').value=type;$('#dateInput').value=a.date;$('#timeInput').value=a.time||'08:00';$('#endTimeInput').value=a.end||'';$('#noteInput').value=a.note||'';$('#reminderInput').value=String(a.reminder||30);typeFields();open('entryModal')}
$('#saveEntry').onclick=()=>{const title=$('#titleInput').value.trim();if(!title)return $('#titleInput').focus();const type=$('#typeInput').value,b={id:editing?.id||uid(),personId:$('#personInput').value,title,date:$('#dateInput').value||iso(nowDay()),note:$('#noteInput').value.trim()};if(editing){data.events=data.events.filter(x=>x.id!==editing.id);data.tasks=data.tasks.filter(x=>x.id!==editing.id)}if(type==='event')data.events.push({...b,time:$('#timeInput').value||'08:00',end:$('#endTimeInput').value||'',reminder:Number($('#reminderInput').value||0)});else data.tasks.push({...b,done:false});save();editing=null;close('entryModal');renderAll();toast('Eintrag gespeichert')};$('#deleteEntry').onclick=()=>{if(!editing)return;if(editing.type==='event')data.events=data.events.filter(x=>x.id!==editing.id);else data.tasks=data.tasks.filter(x=>x.id!==editing.id);save();editing=null;close('entryModal');renderAll();toast('Eintrag gelöscht')};$('#completeTasks').onclick=()=>{const ds=iso(nowDay());data.tasks.forEach(t=>{if(t.date===ds&&(filter==='all'||t.personId===filter))t.done=true});save();renderToday();toast('Aufgaben erledigt')};
$('#addScheduleBtn').onclick=()=>$('#addRuleBtn').click();$('#addRuleBtn').onclick=()=>{$('#ruleTitle').value='';$('#ruleDay').value=String(nowDay().getDay());$('#ruleTime').value='08:00';$('#ruleEnd').value='12:00';$('#ruleNote').value='';open('ruleModal')};$('#saveRule').onclick=()=>{const title=$('#ruleTitle').value.trim();if(!title)return $('#ruleTitle').focus();data.rules.push({id:uid(),personId:$('#rulePerson').value,title,day:Number($('#ruleDay').value),time:$('#ruleTime').value,end:$('#ruleEnd').value,note:$('#ruleNote').value.trim()});save();close('ruleModal');renderWeek();renderPlan();renderToday();toast('Wochenplan gespeichert')};$('#addPersonBtn').onclick=()=>{$('#personName').value='';open('personModal')};$('#savePerson').onclick=()=>{const n=$('#personName').value.trim();if(!n)return;data.people.push({id:'p'+uid(),name:n,color:COLORS[data.people.length%COLORS.length]});save();close('personModal');renderAll();toast('Person hinzugefügt')};$('#prepMinutes').onchange=()=>{data.settings.prep=Number($('#prepMinutes').value||0);save();renderToday()};$('#travelMinutes').onchange=()=>{data.settings.travel=Number($('#travelMinutes').value||0);save();renderToday()};$('#pickupMinutes').onchange=()=>{data.settings.pickup=Number($('#pickupMinutes').value||0);save();renderToday()};$('#eveningHour').onchange=()=>{data.settings.evening=$('#eveningHour').value||'19:00';save();renderToday()};
$('#exportBtn').onclick=()=>{const b=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='family-command-backup-'+iso(nowDay())+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};$('#importInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const x=JSON.parse(await f.text());if(!x.people||!x.events||!x.tasks)throw 0;data=x;save();filter='all';renderAll();toast('Backup importiert')}catch{alert('Ungültiges Backup.')}e.target.value=''};$('#resetBtn').onclick=()=>{if(confirm('Auf Beispieldaten zurücksetzen?')){data=seed();filter='all';save();renderAll();toast('Zurückgesetzt')}};
function updateNotifications(){if(!('Notification'in window)){if($('#notificationState'))$('#notificationState').textContent='Nicht unterstützt';if($('#smartReminderState'))$('#smartReminderState').textContent='Nur In-App';return}const label=Notification.permission==='granted'?'Aktiv · App geöffnet':Notification.permission==='denied'?'Blockiert':'Noch nicht aktiviert';if($('#notificationState'))$('#notificationState').textContent=label;if($('#smartReminderState'))$('#smartReminderState').textContent=Notification.permission==='granted'?'Benachrichtigungen aktiv':'Plan aktiv'}$('#notificationBtn').onclick=async()=>{if('Notification'in window){await Notification.requestPermission();updateNotifications();checkSmartNotifications()}};$('#testNotificationBtn').onclick=()=>{if(!('Notification'in window))return toast('Benachrichtigungen nicht unterstützt');if(Notification.permission!=='granted')return toast('Bitte zuerst aktivieren');new Notification('Family Command Test',{body:'Benachrichtigungen funktionieren, solange die App geöffnet ist.'});toast('Test gesendet')};setInterval(checkSmartNotifications,15000);
const DB='fc-docs',STORE='docs';let scanDocId=null,ocrWorker=null;function db(){return new Promise((r,j)=>{const q=indexedDB.open(DB,1);q.onupgradeneeded=()=>q.result.createObjectStore(STORE,{keyPath:'id'});q.onsuccess=()=>r(q.result);q.onerror=()=>j(q.error)})}async function docs(){try{const d=await db();return await new Promise((r,j)=>{const q=d.transaction(STORE).objectStore(STORE).getAll();q.onsuccess=()=>r(q.result.sort((a,b)=>b.created.localeCompare(a.created)));q.onerror=()=>j(q.error)})}catch{return[]}}async function getDoc(id){const d=await db();return await new Promise((r,j)=>{const q=d.transaction(STORE).objectStore(STORE).get(Number(id));q.onsuccess=()=>r(q.result);q.onerror=()=>j(q.error)})}async function putDoc(x){const d=await db(),tx=d.transaction(STORE,'readwrite');tx.objectStore(STORE).put(x);await new Promise(r=>tx.oncomplete=r)}async function addDocs(fs){if(!fs.length)return;const d=await db(),tx=d.transaction(STORE,'readwrite'),s=tx.objectStore(STORE);fs.forEach(f=>s.put({id:uid(),name:f.name,type:f.type,size:f.size,created:new Date().toISOString(),blob:f,text:''}));await new Promise(r=>tx.oncomplete=r);renderDocs();toast('Dokument gespeichert')}async function renderDocs(){const a=await docs();$('#docCount').textContent=a.length+' Datei'+(a.length===1?'':'en');$('#docList').innerHTML=a.length?a.map(d=>`<div class="doc"><div class="thumb" id="th${d.id}">${d.type.startsWith('image/')?'':'PDF'}</div><div><div class="docName">${esc(d.name)}</div><div class="meta">${Math.round(d.size/1024)} KB · ${new Intl.DateTimeFormat('de-CH').format(new Date(d.created))}${d.text?' · Text erkannt':''}</div></div><div class="docActions"><button class="scanBtn" data-analyze-doc="${d.id}">${d.text?'Neu lesen':'Analysieren'}</button><button class="moreBtn" data-del-doc="${d.id}">✕</button></div></div>`).join(''):'<div class="empty">Noch keine Dokumente gespeichert.</div>';for(const d of a.filter(x=>x.type.startsWith('image/'))){const i=document.createElement('img'),u=URL.createObjectURL(d.blob);i.src=u;i.onload=()=>URL.revokeObjectURL(u);$('#th'+d.id).appendChild(i)}$$('[data-analyze-doc]').forEach(b=>b.onclick=()=>analyzeDocument(Number(b.dataset.analyzeDoc)));$$('[data-del-doc]').forEach(b=>b.onclick=async()=>{const d=await db(),tx=d.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(Number(b.dataset.delDoc));await new Promise(r=>tx.oncomplete=r);renderDocs()})}
function setScanProgress(p,msg){$('#scanProgress').style.width=Math.max(3,Math.round(p*100))+'%';if(msg)$('#scanStatusText').textContent=msg}async function loadTesseract(){if(window.Tesseract)return;setScanProgress(.06,'OCR-Bibliothek wird geladen …');await new Promise((resolve,reject)=>{const x=document.createElement('script');x.src='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';x.onload=resolve;x.onerror=()=>reject(new Error('OCR-Bibliothek konnte nicht geladen werden. Internetverbindung prüfen.'));document.head.appendChild(x)})}async function getOcrWorker(){if(ocrWorker)return ocrWorker;await loadTesseract();setScanProgress(.0