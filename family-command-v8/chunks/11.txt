s="tiny">Raster prüfen → wiederkehrende Woche übernehmen</div></div><button class="moreBtn" id="ttClose">✕</button></div><div class="ttScanBanner"><div>▦</div><div><strong>Das ist ein Wochenplan – kein einzelner Termin.</strong><p>Family Command erkennt belegte und freie Felder. Erst nach deiner Bestätigung entstehen wiederkehrende Schulzeiten.</p></div></div><div class="ttScanControls"><label>Person<select id="ttScanPerson"></select></label><label>Erkennung<select id="ttModeInfo" disabled><option>Farbraster + OCR</option></select></label></div><div class="ttScanSummary" id="ttScanSummary"></div><div class="ttScanGridWrap"><div class="ttScanGrid" id="ttScanGrid"></div></div><div class="smartSection"><div class="smartSectionHead"><h4>Erkannte Schulblöcke</h4><span id="ttBlockCount"></span></div><div class="ttBlocks" id="ttBlocks"></div></div><label class="ttReplace"><input type="checkbox" id="ttReplaceRules" checked><span>Bestehende feste Wochenzeiten der ausgewählten Person ersetzen. Deaktivieren, wenn die erkannten Zeiten nur ergänzt werden sollen.</span></label><details class="ttRaw"><summary>OCR-Text anzeigen</summary><textarea class="rawBox" id="ttRawText" readonly></textarea></details><div class="ttError hidden" id="ttWarning"></div><div class="modalActions"><button class="btn" id="ttUseNormal">Als normalen Zettel behandeln</button><button class="btn primary" id="ttSave">Stundenplan übernehmen</button></div></div>`;document.body.appendChild(el);$('#ttClose').onclick=()=>close('ttScanModal');el.addEventListener('click',e=>{if(e.target===el)close('ttScanModal')});$('#ttUseNormal').onclick=()=>{const text=ttState?.text||'';close('ttScanModal');open('scanModal');$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');fillScan(text)};$('#ttSave').onclick=ttSaveRules}
function ttRenderGrid(){if(!ttState)return;const {matrix,times}=ttState;let h='<div class="ttScanCell ttScanHead">Zeit</div>'+TT_DAYS.map(d=>`<div class="ttScanCell ttScanHead">${d}</div>`).join('');for(let r=0;r<8;r++){h+=`<div class="ttScanCell ttScanTime">${times[r][0]}<br>– ${times[r][1]}</div>`;for(let c=0;c<5;c++)h+=`<div class="ttScanCell"><button type="button" class="ttScanToggle ${matrix[r][c]?'on':''}" data-tt-cell="${r}:${c}">${matrix[r][c]?'Unterricht':'frei'}</button></div>`}$('#ttScanGrid').innerHTML=h;$$('[data-tt-cell]').forEach(b=>b.onclick=()=>{const [r,c]=b.dataset.ttCell.split(':').map(Number);ttState.matrix[r][c]=!ttState.matrix[r][c];ttState.blocks=ttBlocksFromMatrix(ttState.matrix,ttState.times);ttRenderGrid();ttRenderBlocks();ttRenderSummary()})}
function ttRenderBlocks(){if(!ttState)return;$('#ttBlocks').innerHTML=ttState.blocks.map((b,i)=>`<div class="ttBlock ${b.enabled?'':'off'}"><button type="button" class="ttBlockToggle" data-tt-block-toggle="${i}">${b.enabled?'✓':''}</button><div class="ttBlockMain"><b>${TT_DAYS[b.day-1]} · ${esc(b.title)}</b><span>${b.time}–${b.end}</span></div><div class="ttBlockEdit"><input data-tt-block-title="${i}" value="${esc(b.title)}" aria-label="Titel"><input type="time" data-tt-block-time="${i}" value="${b.time}"><input type="time" data-tt-block-end="${i}" value="${b.end}"></div></div>`).join('')||'<div class="empty">Keine Unterrichtszeiten ausgewählt.</div>';$$('[data-tt-block-toggle]').forEach(x=>x.onclick=()=>{const b=ttState.blocks[Number(x.dataset.ttBlockToggle)];b.enabled=!b.enabled;ttRenderBlocks();ttRenderSummary()});$$('[data-tt-block-title]').forEach(x=>x.oninput=()=>ttState.blocks[Number(x.dataset.ttBlockTitle)].title=x.value);$$('[data-tt-block-time]').forEach(x=>x.oninput=()=>ttState.blocks[Number(x.dataset.ttBlockTime)].time=x.value);$$('[data-tt-block-end]').forEach(x=>x.oninput=()=>ttState.blocks[Number(x.dataset.ttBlockEnd)].end=x.value);$('#ttBlockCount').textContent=`${ttState.blocks.filter(b=>b.enabled).length} Blöcke`}
function ttRenderSummary(){if(!ttState)return;const selected=ttState.blocks.filter(b=>b.enabled).length,source=ttState.timeSource==='ocr'?'Zeiten per OCR':ttState.timeSource==='mixed'?'Zeiten teilweise OCR':'Zeiten aus Schulraster – prüfen';$('#ttScanSummary').innerHTML=`<span class="good">Stundenplan erkannt · ${ttState.confidence}%</span><span class="good">${ttState.occupied} belegte Felder</span><span>${selected} Schulblöcke</span><span class="${ttState.timeSource==='template'?'warn':''}">${source}</span>`;$('#ttWarning').classList.toggle('hidden',ttState.timeSource!=='template');if(ttState.timeSource==='template')$('#ttWarning').textContent='Die OCR hat nicht alle acht Zeitzeilen sicher gelesen. Deshalb wurden die im fotografierten Raster erkennbaren Standardzeiten 07:30–16:05 eingesetzt. Bitte die Zeiten vor dem Speichern kurz prüfen.'}
function ttShowReview(doc,text,grid){ttEnsureModal();const tm=ttChooseTimes(text),personId=ttPersonFromText(text);ttState={docId:doc.id,text,grid,matrix:grid.matrix.map(r=>r.slice()),times:tm.times,timeSource:tm.source,confidence:grid.confidence||80,occupied:grid.occupied,blocks:[]};ttState.blocks=ttBlocksFromMatrix(ttState.matrix,ttState.times);$('#ttScanPerson').innerHTML='<option value="">Person auswählen …</option>'+data.people.map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join('');$('#ttScanPerson').value=personId;$('#ttRawText').value=text||'(OCR-Text konnte nicht zuverlässig gelesen werden – das Raster wurde trotzdem erkannt.)';ttRenderGrid();ttRenderBlocks();ttRenderSummary();close('scanModal');open('ttScanModal')}
function ttSaveRules(){if(!ttState)return;const pid=$('#ttScanPerson').value;if(!pid){$('#ttScanPerson').focus();toast('Bitte zuerst die Person auswählen');return}const chosen=ttState.blocks.filter(b=>b.enabled&&b.title.trim()&&b.time&&b.end);if(!chosen.length){toast('Keine Schulzeiten ausgewählt');return}if($('#ttReplaceRules').checked)data.rules=data.rules.filter(r=>r.personId!==pid);for(const b of chosen)data.rules.push({id:uid(),personId:pid,title:b.title.trim(),day:b.day,time:b.time,end:b.end,note:'Aus Stundenplan-Scan',source:'timetable-scan'});save();close('ttScanModal');renderAll();setView('plan');toast(`${chosen.length} Stundenplan-Blöcke übernommen`)}

const oldAnalyzeDocument=analyzeDocument;
analyzeDocument=async function(id){scanDocId=id;syncSelects();$('#scanBusy').classList.remove('hidden');$('#scanReview').classList.add('hidden');setScanProgress(.03,'Dokumenttyp wird erkannt …');open('scanModal');try{const d=await getDoc(id);if(!d)throw new Error('Dokument nicht gefunden.');if(d.type.startsWith('image/')){setScanProgress(.08,'Stundenplan-Raster wird geprüft …');const grid=await ttInspectColoredGrid(d.blob);if(grid.detected){setScanProgress(.26,'Stundenplan erkannt – Beschriftungen werden gelesen …');let text='';try{text=await ocrImage(d.blob)}catch(e){console.warn('OCR optional failed',e)}d.text=text;await putDoc(d);setScanProgress(1,'Stundenplan erkannt');renderDocs();ttShowReview(d,text,grid);return}}
// Kein visuell sicherer Stundenplan: normale Dokumentanalyse
let text='';if(d.type.startsWith('image/'))text=await ocrImage(d.blob);else if(d.type==='application/pdf')text=await extractPdf(d.blob);else throw new Error('Dieser Dateityp wird nicht unterstützt.');if(!text.trim())throw new Error('Es konnte kein lesbarer Text erkannt werden.');d.text=text;await putDoc(d);setScanProgress(1,'Fertig');fillScan(text);$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');renderDocs()}catch(e){console.error(e);$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');$('#scanRaw').value='';smartAnalysis={facts:{title:'Analyse fehlgeschlagen',date:'',start:'',end:'',personId:data.people[0]?.id,location:'',money:'',pack:[]},signals:{},proposals:[]};$('#smartFacts').innerHTML=fact('Fehler',e.message||'Analyse fehlgeschlagen');$('#scanConfidence').innerHTML=`<span class="warn">${esc(e.message||'Analyse fehlgeschlagen')}</span>`;$('#proposalList').innerHTML='<div class="empty">Bitte Originaltext unten korrigieren oder erneut scannen.</div>';$('#proposalCount').textContent='0 ausgewählt';toast('Analyse nicht möglich')}};

// OCR-Text, der eindeutig ein Stundenplan ist, darf ebenfalls nicht mehr blind zum Datumstermin werden.
const oldFillScan=fillScan;
fillScan=function(text){if(ttTextScore(text)>=7&&scanDocId){getDoc(scanDocId).then(async d=>{if(d?.type?.startsWith('image/')){const g=await ttInspectColoredGrid(d.blob);if(g.detected){ttShowReview(d,text,g);return}oldFillScan(text)}else oldFillScan(text)}).catch(()=>oldFillScan(text));return}oldFillScan(text)};

// Alte Service Worker entfernen, damit Scanner-Updates auf dem iPhone nicht im Cache hängen bleiben.
if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(r=>r.unregister().catch(()=>{}))).catch(()=>{});
document.documentElement.dataset.fcBuild=TT_BUILD;
})();
</script>
</body></html>
