end):''):''}${p.note?' · '+esc(p.note):''}</div><div class="proposalEdit"><input class="pTitle" data-pfield="title" data-pidx="${i}" value="${esc(p.title)}" aria-label="Titel"><input data-pfield="date" data-pidx="${i}" type="date" value="${esc(p.date)}" aria-label="Datum">${p.kind==='event'?`<input data-pfield="time" data-pidx="${i}" type="time" value="${esc(p.time)}" aria-label="Uhrzeit">`:''}<input class="proposalNote" data-pfield="note" data-pidx="${i}" value="${esc(p.note)}" aria-label="Hinweis"></div></div><span class="proposalTag ${p.kind==='task'?'task':''}">${p.kind==='event'?'Termin':'Aufgabe'}</span></div>`).join('');$$('[data-toggle-proposal]').forEach(b=>b.onclick=()=>{const p=smartAnalysis.proposals[Number(b.dataset.toggleProposal)];p.enabled=!p.enabled;renderProposals()});$$('[data-pfield]').forEach(inp=>inp.oninput=()=>{smartAnalysis.proposals[Number(inp.dataset.pidx)][inp.dataset.pfield]=inp.value});const n=smartAnalysis.proposals.filter(p=>p.enabled).length;$('#proposalCount').textContent=`${n} ausgewählt`;$('#saveScan').disabled=!n}
function fillScan(text){$('#scanRaw').value=text;renderSmartReview(interpretSmart(text))}
async function analyzeTextDirect(text){scanDocId=null;syncSelects();$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');open('scanModal');fillScan(text)}
async function analyzeDocument(id){scanDocId=id;syncSelects();$('#scanBusy').classList.remove('hidden');$('#scanReview').classList.add('hidden');setScanProgress(.03,'Dokument wird vorbereitet …');open('scanModal');try{const d=await getDoc(id);if(!d)throw new Error('Dokument nicht gefunden.');let text='';if(d.type.startsWith('image/'))text=await ocrImage(d.blob);else if(d.type==='application/pdf')text=await extractPdf(d.blob);else throw new Error('Dieser Dateityp wird nicht unterstützt.');if(!text.trim())throw new Error('Es konnte kein lesbarer Text erkannt werden.');d.text=text;await putDoc(d);setScanProgress(1,'Fertig');fillScan(text);$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');renderDocs()}catch(e){$('#scanBusy').classList.add('hidden');$('#scanReview').classList.remove('hidden');$('#scanRaw').value='';smartAnalysis={facts:{title:'Analyse fehlgeschlagen',date:'',start:'',end:'',personId:data.people[0]?.id,location:'',money:'',pack:[]},signals:{},proposals:[]};$('#smartFacts').innerHTML=fact('Fehler',e.message||'Analyse fehlgeschlagen');$('#scanConfidence').innerHTML=`<span class="warn">${esc(e.message||'Analyse fehlgeschlagen')}</span>`;$('#proposalList').innerHTML='<div class="empty">Bitte Originaltext unten korrigieren oder erneut scannen.</div>';$('#proposalCount').textContent='0 ausgewählt';toast('Analyse nicht möglich')}}
$('#reanalyzeText').onclick=()=>fillScan($('#scanRaw').value);$('#saveScan').onclick=()=>{if(!smartAnalysis)return;const chosen=smartAnalysis.proposals.filter(p=>p.enabled&&p.title.trim());if(!chosen.length)return;for(const p of chosen){const b={id:uid(),personId:p.personId,title:p.title.trim(),date:p.date||iso(nowDay()),note:(p.note||'').trim(),source:'document'};if(p.kind==='event')data.events.push({...b,time:p.time||'08:00',end:p.end||'',reminder:30});else data.tasks.push({...b,done:false})}save();close('scanModal');renderAll();setView('today');toast(`${chosen.length} Einträge übernommen`)};
$('#demoScanBtn').onclick=()=>analyzeTextDirect(`Schulreise\nKind 2\n20. August 2026\n07:00 - 17:00 Uhr\nTreffpunkt Bahnhof\nBitte 5 Franken mitgeben. Gute Schuhe, Trinkflasche und Rucksack mitnehmen.`);
$('#cameraBtn').onclick=e=>{e.preventDefault();e.stopPropagation();$('#cameraInput').click()};$('#cameraInput').onchange=e=>{addDocs([...e.target.files]);e.target.value=''};$('#fileInput').onchange=e=>{addDocs([...e.target.files]);e.target.value=''};['dragover','dragenter'].forEach(n=>$('#drop').addEventListener(n,e=>{e.preventDefault();$('#drop').classList.add('drag')}));['dragleave','drop'].forEach(n=>$('#drop').addEventListener(n,e=>{e.preventDefault();$('#drop').classList.remove('drag')}));$('#drop').addEventListener('drop',e=>addDocs([...e.dataTransfer.files].filter(f=>f.type.startsWith('image/')||f.type==='application/pdf')));

let authMode='login';function setAuthMode(mode){authMode=mode;$('#authLoginTab').classList.toggle('active',mode==='login');$('#authSignupTab').classList.toggle('active',mode==='signup');$('#authSubmit').textContent=mode==='login'?'Anmelden':'Konto erstellen';$('#authPassword').autocomplete=mode==='login'?'current-password':'new-password';$('#authHint').textContent=mode==='login'?'Melde dich mit demselben Konto auf weiteren Geräten an. Deine Planungsdaten werden automatisch synchronisiert.':'Nach der Registrierung kann eine E-Mail-Bestätigung nötig sein.';$('#authError').classList.remove('show')}
function showAuthError(text){const e=$('#authError');e.textContent=text;e.classList.add('show')}
$('#syncBadge').onclick=()=>{renderAccountState();open('accountModal')};$('#accountBtn').onclick=()=>{renderAccountState();open('accountModal')};$('#authLoginTab').onclick=()=>setAuthMode('login');$('#authSignupTab').onclick=()=>setAuthMode('signup');$('#authSubmit').onclick=async()=>{if(!sb)return showAuthError('Cloud-Verbindung ist noch nicht eingerichtet.');const email=$('#authEmail').value.trim(),password=$('#authPassword').value;if(!email||password.length<8)return showAuthError('Bitte gültige E-Mail und mindestens 8 Zeichen Passwort eingeben.');$('#authSubmit').disabled=true;$('#authError').classList.remove('show');try{if(authMode==='signup'){const {data:r,error}=await sb.auth.signUp({email,password});if(error)throw error;if(!r.session){$('#authHint').textContent='Konto erstellt. Bitte prüfe deine E-Mail und bestätige die Anmeldung.';toast('Bestätigungs-E-Mail gesendet')}else{currentUser=r.user;await startCloudSession();renderAccountState()}}else{const {data:r,error}=await sb.auth.signInWithPassword({email,password});if(error)throw error;currentUser=r.user;await startCloudSession();renderAccountState();close('accountModal');toast('Angemeldet')}}catch(e){showAuthError(cloudErrorMessage(e))}finally{$('#authSubmit').disabled=false}};
async function signOutCloud(){if(!sb)return;await sb.auth.signOut();currentUser=null;stopRealtime();renderAccountState();close('accountModal');toast('Abgemeldet')}
$('#signOutBtn').onclick=signOutCloud;$('#accountSignOutBtn').onclick=signOutCloud;$('#syncNowBtn').onclick=()=>pullOrSeedCloud();$('#accountSyncBtn').onclick=()=>pullOrSeedCloud();$('#resetPasswordBtn').onclick=async()=>{if(!sb)return showAuthError('Cloud-Verbindung ist noch nicht eingerichtet.');const email=$('#authEmail').value.trim();if(!email)return showAuthError('Bitte zuerst deine E-Mail eingeben.');const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:location.origin+location.pathname});if(error)return showAuthError(cloudErrorMessage(error));$('#authHint').textContent='E-Mail zum Zurücksetzen wurde angefordert.';toast('E-Mail angefordert')};

function toast(x){const t=$('#toast');t.textContent=x;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1500)}window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();installPrompt=e;$('#installBtn').hidden=false});$('#installBtn').onclick=async()=>{if(installPrompt){installPrompt.prompt();await installPrompt.userChoice;installPrompt=null;$('#installBtn').hidden=true}};/* V8: alter Service Worker absichtlich deaktiviert */renderAll();updateNotifications();checkSmartNotifications();renderAccountState();initCloud().catch(e=>{setSyncState('offline','Cloud-Fehler');logSync(cloudErrorMessage(e))});
</script>\n<style id="fc-tt-v8-css">
.ttScanBanner{display:flex;gap:12px;align-items:flex-start;background:linear-gradient(135deg,#eeedff,#f7f7ff);border:1px solid #d7d3ff;border-radius:18px;padding:14px;margin-bottom:14px}.ttScanBanner strong{display:block;color:#4238bd;font-size:15px}.ttScanBanner p{margin:4px 0 0;color:#5f6280;font-size:11px;line-height:1.45}.ttScanControls{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:12px 0}.ttScanControls label{display:grid;gap:5px;font-size:10px;font-weight:850;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.ttScanControls select{width:100%;border:1px solid var(--line);border-radius:12px;padding:10px;background:#fff;color:var(--text);font-weight:750;text-transform:none;letter-spacing:0}.ttScanSummary{display:flex;gap:7px;flex-wrap:wrap;margin:10px 0 14px}.ttScanSummary span{padding:7px 9px;border-radius:999px;background:var(--s2);font-size:10px;font-weight:800;color:var(--muted)}.ttScanSummary .good{background:var(--good2);color:var(--good)}.ttScanSummary .warn{background:var(--warn2);color:var(--warn)}.ttScanGridWrap{overflow:auto;border:1px solid var(--line);border-radius:16px;background:#fff}.ttScanGrid{display:grid;grid-template-columns:92px repeat(5,minmax(92px,1fr));min-width:600px}.ttScanCell{min-height:58px;border-right:1px solid var(--line);border-bottom:1px solid var(--line);display:flex;align-i