8,'Deutsches OCR-Modell wird geladen …');ocrWorker=await Tesseract.createWorker('deu',undefined,{logger:m=>{if(m.status==='recognizing text')setScanProgress(.2+.75*(m.progress||0),'Text wird erkannt …')}});return ocrWorker}async function ocrImage(input){const w=await getOcrWorker();const r=await w.recognize(input);return (r.data&&r.data.text||'').trim()}async function extractPdf(blob){setScanProgress(.08,'PDF-Modul wird geladen …');const pdfjs=await import('https://cdn.jsdelivr.net/npm/pdfjs-dist/+esm');pdfjs.GlobalWorkerOptions.workerSrc='https://cdn.jsdelivr.net/npm/pdfjs-dist/build/pdf.worker.min.mjs';const pdf=await pdfjs.getDocument({data:await blob.arrayBuffer()}).promise;let out='';const max=Math.min(pdf.numPages,3);for(let i=1;i<=max;i++){setScanProgress(.1+i/max*.45,`PDF-Seite ${i} von ${max} wird gelesen …`);const page=await pdf.getPage(i),content=await page.getTextContent();out+=content.items.map(x=>x.str).join(' ')+'\n'}if(out.replace(/\s/g,'').length<25){setScanProgress(.55,'Scan-PDF erkannt – erste Seite wird per OCR gelesen …');const page=await pdf.getPage(1),viewport=page.getViewport({scale:1.7}),canvas=document.createElement('canvas');canvas.width=Math.ceil(viewport.width);canvas.height=Math.ceil(viewport.height);await page.render({canvasContext:canvas.getContext('2d'),viewport}).promise;out=await ocrImage(canvas)}return out.trim()}
const MONTHS={januar:1,februar:2,märz:3,maerz:3,april:4,mai:5,juni:6,juli:7,august:8,september:9,oktober:10,november:11,dezember:12};function parseDateText(text){const n=new Date(),year=n.getFullYear();let m=text.match(/\b(\d{1,2})[.\/-](\d{1,2})(?:[.\/-](\d{2,4}))?\b/);if(m){let y=m[3]?Number(m[3]):year;if(y<100)y+=2000;const mm=Number(m[2]),dd=Number(m[1]);if(mm>=1&&mm<=12&&dd>=1&&dd<=31)return`${y}-${String(mm).padStart(2,'0')}-${String(dd).padStart(2,'0')}`}m=text.toLowerCase().match(/\b(\d{1,2})\.?\s*(januar|februar|märz|maerz|april|mai|juni|juli|august|september|oktober|november|dezember)(?:\s+(\d{4}))?\b/);if(m){const y=Number(m[3]||year),mm=MONTHS[m[2]],dd=Number(m[1]);return`${y}-${String(mm).padStart(2,'0')}-${String(dd).padStart(2,'0')}`}return iso(nowDay())}function parseTimeRange(text){const clean=text.replace(/[–—]/g,'-');let m=clean.match(/\b([01]?\d|2[0-3])[:.]([0-5]\d)\s*(?:uhr)?\s*(?:-|bis)\s*([01]?\d|2[0-3])[:.]([0-5]\d)\s*(?:uhr)?\b/i);if(m)return{start:`${String(Number(m[1])).padStart(2,'0')}:${m[2]}`,end:`${String(Number(m[3])).padStart(2,'0')}:${m[4]}`};m=clean.match(/\b(?:von\s*)?([01]?\d|2[0-3])\s*(?:uhr)?\s*(?:-|bis)\s*([01]?\d|2[0-3])\s*uhr\b/i);if(m)return{start:`${String(Number(m[1])).padStart(2,'0')}:00`,end:`${String(Number(m[2])).padStart(2,'0')}:00`};m=clean.match(/\b(?:um\s*)?([01]?\d|2[0-3])[:.]([0-5]\d)\s*(?:uhr)?\b/i);if(m)return{start:`${String(Number(m[1])).padStart(2,'0')}:${m[2]}`,end:''};m=clean.match(/\b(?:um|ab|von)\s+([01]?\d|2[0-3])\s*uhr\b/i);return m?{start:`${String(Number(m[1])).padStart(2,'0')}:00`,end:''}:{start:'08:00',end:''}}
function inferTitle(text){const low=text.toLowerCase(),map=[['schulreise','Schulreise'],['klassenfahrt','Klassenfahrt'],['ausflug','Ausflug'],['elternabend','Elternabend'],['sporttag','Sporttag'],['fototermin','Fototermin'],['schulfoto','Schulfoto'],['zahnarzt','Zahnarzt'],['arzttermin','Arzttermin'],['kindergarten','Kindergarten'],['schule','Schule']];for(const[k,v]of map)if(low.includes(k))return v;const line=text.split(/\n+/).map(x=>x.trim()).find(x=>x.length>=4&&x.length<=70);return line?line.replace(/\s+/g,' '):'Neuer Termin'}function inferPerson(text){const low=text.toLowerCase();return data.people.find(p=>low.includes(p.name.toLowerCase()))?.id||data.people[0]?.id}function inferNote(text){return text.replace(/\s+/g,' ').trim().slice(0,320)}
function inferLocation(text){const pats=[/(?:treffpunkt|besammlung|ort)\s*[:\-]?\s*([^\n.;]{3,80})/i,/(?:wir treffen uns|treffen wir uns)\s+(?:am|beim|bei|vor|in)\s+([^\n.;]{3,70})/i];for(const p of pats){const m=text.match(p);if(m)return m[1].trim().replace(/\s{2,}/g,' ')}return''}
function inferMoney(text){const m=text.match(/\b(\d{1,3}(?:[.,]\d{1,2})?)\s*(?:chf|franken|fr\.?)(?:\b|\s)/i)||text.match(/(?:chf|fr\.?)\s*(\d{1,3}(?:[.,]\d{1,2})?)/i);return m?m[1].replace('.',',')+' CHF':''}
function inferPackItems(text){const low=text.toLowerCase();const vocab=[['gute schuhe','gute Schuhe'],['wanderschuhe','Wanderschuhe'],['turnschuhe','Turnschuhe'],['regenjacke','Regenjacke'],['sonnenhut','Sonnenhut'],['sonnencreme','Sonnencreme'],['trinkflasche','Trinkflasche'],['wasserflasche','Wasserflasche'],['znüni','Znüni'],['znueni','Znüni'],['lunch','Lunch'],['mittagessen','Mittagessen'],['rucksack','Rucksack'],['sportzeug','Sportzeug'],['turnzeug','Turnzeug'],['badehose','Badehose'],['badesachen','Badesachen'],['helm','Helm'],['leuchtweste','Leuchtweste'],['stifte','Stifte'],['etui','Etui']];const found=[];for(const[k,v]of vocab)if(low.includes(k)&&!found.includes(v))found.push(v);const m=text.match(/(?:mitnehmen|mitbringen|einpacken|braucht ihr|benötigt(?: werden)?)[\s:,-]+([^\n.]{3,140})/i);if(m){m[1].split(/,|;|\bund\b|\//i).map(x=>x.trim().replace(/^(bitte|einen?|eine|das|die|der)\s+/i,'')).filter(x=>x.length>2&&x.length<45).slice(0,6).forEach(x=>{const clean=x.replace(/\s+/g,' ');if(!found.some(f=>f.toLowerCase()===clean.toLowerCase()))found.push(clean.charAt(0).toUpperCase()+clean.slice(1))})}return found.slice(0,8)}
function beforeDate(ds,days=1){const d=new Date(ds+'T12:00:00');d.setDate(d.getDate()-days);const candidate=iso(d),today=iso(nowDay());return candidate<today?today:candidate}
function interpretSmart(text){const date=parseDateText(text),times=parseTimeRange(text),title=inferTitle(text),personId=inferPerson(text),location=inferLocation(text),money=inferMoney(text),pack=inferPackItems(text);const low=text.toLowerCase();const hasDate=date!==iso(nowDay())||/\d{1,2}[.\/-]\d{1,2}|januar|februar|märz|maerz|april|mai|juni|juli|august|september|oktober|november|dezember/i.test(text),hasTime=/\b([01]?\d|2[0-3])[:.]([0-5]\d)|\b(?:um|ab|von)\s+([01]?\d|2[0-3])\s*uhr/i.test(text),personFound=data.people.some(p=>low.includes(p.name.toLowerCase()));const proposals=[];proposals.push({id:'p'+uid(),kind:'event',enabled:true,title,date,time:times.start,end:times.end,note:[location&&`Ort: ${location}`,times.end&&`Ende: ${times.end}`].filter(Boolean).join(' · '),personId});if(pack.length)proposals.push({id:'p'+uid(),kind:'task',enabled:true,title:`Für ${title} einpacken`,date:beforeDate(date),time:'',end:'',note:pack.join(' · '),personId});if(money)proposals.push({id:'p'+uid(),kind:'task',enabled:true,title:`${money} bereitlegen`,date:beforeDate(date),time:'',end:'',note:`Für ${title}`,personId});if(/unterschrift|unterschreiben|talon|anmeldung|anmelden|formular/i.test(text))proposals.push({id:'p'+uid(),kind:'task',enabled:true,title:`Rückmeldung für ${title}`,date:beforeDate(date),time:'',end:'',note:'Unterschrift / Anmeldung prüfen',personId});if(times.end&&/(abholen|abholung|rückkehr|rueckkehr|zurück|zurueck|ende)/i.test(text))proposals.push({id:'p'+uid(),kind:'event',enabled:false,title:`${title}: Ende / Abholen`,date,time:times.end,end:'',note:location?`Ort: ${location}`:'',personId});return{facts:{title,date,start:times.start,end:times.end,personId,location,money,pack},signals:{hasDate,hasTime,personFound},proposals}}
let smartAnalysis=null;function fact(label,value){return`<div class="fact"><span>${label}</span><b title="${esc(value||'Nicht erkannt')}">${esc(value||'Nicht erkannt')}</b></div>`}function renderSmartReview(a){smartAnalysis=a;const f=a.facts;$('#smartFacts').innerHTML=[fact('Titel',f.title),fact('Person',person(f.personId).name),fact('Datum',fmt(f.date,{day:'2-digit',month:'2-digit',year:'numeric'})),fact('Zeit',f.end?`${f.start}–${f.end}`:f.start),fact('Ort',f.location||'—'),fact('Geld',f.money||'—')].join('')+(f.pack.length?`<div class="fact" style="grid-column:1/-1"><span>Mitnehmen</span><b style="white-space:normal">${esc(f.pack.join(' · '))}</b></div>`:'');$('#scanConfidence').innerHTML=`<span class="${a.signals.hasDate?'ok':'warn'}">${a.signals.hasDate?'Datum erkannt':'Datum prüfen'}</span><span class="${a.signals.hasTime?'ok':'warn'}">${a.signals.hasTime?'Zeit erkannt':'Zeit prüfen'}</span><span class="${a.signals.personFound?'ok':'warn'}">${a.signals.personFound?'Person erkannt':'Person prüfen'}</span>${f.pack.length?'<span class="ok">Packhinweise erkannt</span>':''}${f.money?'<span class="ok">Geldbetrag erkannt</span>':''}`;renderProposals()}
function renderProposals(){if(!smartAnalysis)return;$('#proposalList').innerHTML=smartAnalysis.proposals.map((p,i)=>`<div class="proposal ${p.enabled?'':'off'}" data-proposal="${i}"><button class="proposalToggle" type="button" data-toggle-proposal="${i}">${p.enabled?'✓':''}</button><div><div class="proposalTitle">${esc(p.title)}</div><div class="proposalMeta">${esc(person(p.personId).name)} · ${esc(fmt(p.date,{day:'2-digit',month:'2-digit',year:'numeric'}))}${p.kind==='event'?' · '+esc(p.time)+(p.end?'–'+esc(p.